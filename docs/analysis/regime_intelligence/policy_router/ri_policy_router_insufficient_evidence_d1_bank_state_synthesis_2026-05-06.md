# RI policy router insufficient-evidence D1 bank-state synthesis

Date: 2026-05-06
Branch: `feature/next-slice-2026-05-05`
Mode: `RESEARCH`
Status: `completed / docs-only synthesis / bounded D1 bank re-anchor`

This note closes and re-anchors the **current** insufficient-evidence D1 line after three bounded D1-bank phases now exist on the repo-visible surface:

- the completed four-surface D1 synthesis,
- the completed fixed-bank context-clean reread, and
- the completed first fresh late-2024 external-surface falsifier.

It does **not** reopen March as a primary loop.
It does **not** reopen July `2024` as a primary subject.
It does **not** recycle late-2024 as an open transport loop.
It does **not** authorize runtime/default/config/policy/promotion work.

Its purpose is narrower:

> state the smallest honest reading of what the current D1 bank now supports after one bounded internal context-clean reread and one bounded external transport falsifier, and state clearly what still remains out of scope.

## COMMAND PACKET

- **Category:** `obs`
- **Mode:** `RESEARCH` — source: live branch `feature/next-slice-2026-05-05`
- **Risk:** `LOW`
- **Required Path:** `smallest admissible RESEARCH docs-only path`
- **Lane:** `Research-evidence`
- **Objective:** synthesize the current D1 bank state after the completed four-surface recurrence re-anchor, the completed fixed-bank context-clean reread, and the completed first external-surface falsifier.
- **Candidate:** `docs-only D1 bank-state synthesis / re-anchor`
- **Base SHA:** `e958bd3e266830ecbc66150d1e5021c0a68df56c`
- **Skill Usage:** no additional repo skill is claimed for this two-file docs-only synthesis; the evidence inputs summarized here come from previously completed helper-backed slices already run under `python_engineering`
- **Opus pre-code verdict:** not requested on this trivial two-file docs-only quick path

## Evidence inputs

- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_four_surface_synthesis_2026-05-05.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_context_clean_selectivity_falsifier_2026-05-05.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_late_2024_external_surface_falsifier_2026-05-06.md`
- `GENESIS_WORKING_CONTRACT.md`

## Bounded D1 bank recap

### 1. The four-surface chain established a real recurring non-age D1 bank

The completed four-surface synthesis closed the first D1 recurrence phase on one fixed harmful anchor plus three fixed weak-control surfaces:

- harmful `2019-06`
- weak-control `2022-06`
- weak-control `2025-03`
- source-backed weak-control `2020-10/11`

Across that exact bank, the same non-age field family recurred on:

- `action_edge`
- `confidence_gate`
- `clarity_raw`

The honest category there was already stronger than pair-local luck.
It was still exact-surface and observational only.

### 2. The fixed-bank reread showed that the bank is internally context-clean on its frozen surface

The completed context-clean falsifier then asked the next honest bank-level question without widening the subject set:

> if the exact four-surface D1 bank is frozen and reread as one combined target bank versus one combined context bank, do the admitted non-age fields stay below the fixed context floor?

On that exact reread, the answer was bounded non-null on all three admitted non-age fields:

- `action_edge`
- `confidence_gate`
- `clarity_raw`

The tightest constraint on all three fields still came from the harmful-side sibling row rather than from a weak-control context row.
So the bank is not just recurring.
It is also internally coherent on its own fixed frozen surface.

### 3. The first fresh external test already falsified unchanged transport

The completed late-2024 external-surface falsifier then asked the first genuinely new transport question:

> if the exact D1 bank ceilings are transported unchanged onto one fixed late-2024 `5 + 6` external surface, does any admitted claim field survive the exact `5 / 5 target + 0 / 6 anti-target` rubric there?

The answer was no.
The slice failed closed to `external_surface_falsified`.

Exact late-2024 result:

- `action_edge <= 0.033803` -> `4 / 5` target, `0 / 6` anti-target
- `confidence_gate <= 0.516902` -> `4 / 5` target, `0 / 6` anti-target
- `clarity_raw <= 0.364914` -> `not_evaluable` on that external artifact
- `clarity_score <= 36.0` -> `4 / 5` target, `0 / 6` anti-target, but remains descriptive only

So the bank is internally stronger than a pair-local story,
but it is already **not** unchanged-transport clean on the first fresh external holdout.

## Honest synthesis

The smallest honest reading of the current D1 line is now:

> the current insufficient-evidence D1 line is best treated as a bounded evidence bank with one real fixed-bank context-clean reread and one real first-fresh external-surface falsifier: the frozen bank remains internally coherent on `action_edge`, `confidence_gate`, and `clarity_raw`, but unchanged transport is already broken on the late-2024 external surface.

That is the strongest honest sentence.
It is stronger than the earlier four-surface recurrence read.
It is also narrower than transport-clean or runtime-ready authority.

## What the current D1 bank now supports

The current bank now supports all of the following bounded statements:

1. the harmful anchor remains exact `2019-06`
2. the weak-control side remains fixed to exact `2022-06`, exact `2025-03`, and exact source-backed `2020-10/11`
3. the recurring non-age D1 family remains the same:
   - `action_edge`
   - `confidence_gate`
   - `clarity_raw`
4. the fixed-bank reread shows that the target-bank ceilings stay below the fixed context-bank minima on that exact frozen bank
5. the bank therefore has real internal context-clean support on its own locked surface
6. the first fresh external transport check already falsifies unchanged transport on late-2024
7. the correct current category is therefore:
   - exact-bank supported
   - externally falsified once
   - observational only
   - non-authoritative

## What the current D1 bank still does **not** support

The current bank still does **not** support any of the following:

- a portable runtime rule
- a transport-clean external-surface claim
- a globally stable discriminator claim
- a statement that `clarity_raw` is no longer important because it was missing on one external artifact
- a statement that the parked July translation line has been reopened or repaired
- a statement that late-2024 should remain an open holdout loop
- a statement that cheap recurrence expansion should continue by default
- runtime/default/config/policy/family/champion/promotion authority

## Why the category fence matters

The current D1 line is now stronger on one axis and weaker on another:

- stronger internally, because the exact frozen bank survives a context-clean reread,
- weaker externally, because the first fresh unchanged transport already fails.

That combination is exactly why the right category is now:

- bounded research evidence
- exact-bank supported
- not transport-clean
- observational only
- non-authoritative

If that fence is not written explicitly, the line is too easy to misremember as either:

- “still just pair-local,” or
- “already basically portable.”

Neither is true anymore.

## Re-anchor verdict

The current D1 line should now be treated as a **bounded evidence bank state**, not as an automatically widening recurrence loop.

That means:

- do not reopen late-2024 as recycled transport logic
- do not reopen March as a primary loop
- do not reopen July `2024` as a primary subject
- do not silently promote the current bank into runtime or concept authority
- do not keep adding cheap external checks by default without a newly packeted question

## Next admissible step

If this line is continued again after this synthesis, it should start only from a **fresh packet** posing a genuinely new question.

That next question may not be:

- another cheap recurrence check,
- another late-2024 reuse,
- or a disguised restatement of the current bank as if the present synthesis had already granted transport or runtime authority.

The current honest end state of this D1 phase is therefore:

- four-surface recurrence bank fixed
- one bounded context-clean bank reread complete
- one bounded external-surface falsifier complete
- bank stronger than pair-local recurrence
- bank not yet transport-clean
- category unchanged: observational only, non-authoritative

# GENESIS_WORKING_CONTRACT.md

> Working anchor for day-start and resumed sessions.
> This file is **not SSOT** and must not override repo governance documents.

## Purpose

This file exists to prevent session drift.
Before starting new work, the agent should re-anchor against the latest validated lane, known blockers, and next admissible step.

## Non-purpose

This file does **not**:

- authorize new implementation work by itself
- grant execution authority by itself
- override `.github/copilot-instructions.md`, `docs/governance_mode.md`, `AGENTS.md`, or explicit user instructions
- replace packets, reports, or verified evidence artifacts

## Authority order

1. Explicit user request for the current task
2. `.github/copilot-instructions.md`
3. `docs/governance_mode.md`
4. `docs/OPUS_46_GOVERNANCE.md`
5. `AGENTS.md`
6. This file (`GENESIS_WORKING_CONTRACT.md`)

## Current branch and mode anchor

- Branch: `feature/next-slice-2026-04-29`
- Expected mode on this branch: `RESEARCH`
- RESEARCH allows the smallest reproducible, traceable step
- RESEARCH does **not** authorize drift into strict-only surfaces, runtime-default authority, promotion, or champion claims without the required lane/packet

## Core conceptual lock

Genesis must be treated as a **deterministic policy-selection system**.
It is **not** an adaptive system.
It selects among predefined policies based on observable state, and any switching must remain exact, traceable, and reproducible.

## Current validated lane

Active focus right now:

- the new RI target-selection anchor is now `docs/decisions/regime_intelligence/policy_router/ri_policy_router_payoff_state_translation_precode_packet_2026-04-30.md`, which freezes the user's direction choice to continue on **RI rather than Legacy**, preserves payoff-state as the research/evaluation target truth, and keeps decision-time-only RI state as the first admissible runtime carrier
- the active bounded RI-router runtime anchor is now `docs/decisions/regime_intelligence/policy_router/ri_policy_router_continuation_release_hysteresis_runtime_packet_2026-04-30.md`, which implements one enabled-only `continuation_release_hysteresis` seam for `RI_defensive_transition_policy -> RI_continuation_policy`, keeps default/off parity and canonical backcompat intact, and is green on focused plus high-sensitivity selectors with Opus post-diff `APPROVED`
- the newest exact seam-validation anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_continuation_release_hysteresis_carrier_validation_2026-05-04.md`, which reuses the fixed December fail-B RI carrier and shows it is a null validation surface for the seam: top-line metrics are identical, `switch_control_mode == continuation_release` never occurs, and the observed row diffs are parameter-only debug/fingerprint drift rather than behavioral drift
- the newest exercising-subject anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_continuation_release_hysteresis_exercising_subject_2026-05-04.md`, which locks `tBTCUSD` / `3h` / `2024-01-01 -> 2024-01-20` as the first exact repo-visible subject that actually exercises the seam, proves the baseline `continuation_release` cluster on the fixed `2024-01-17 15:00 -> 2024-01-19 06:00` window, and shows that `continuation_release_hysteresis = 0` changes router-state semantics (`11` exercising-row behavioral diffs, `5` policy flips, `1` size change) without changing action count or top-line P&L on that exact subject
- the newest historical inventory anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_continuation_release_hysteresis_monthly_inventory_2026-05-04.md`, which freezes one deterministic full-calendar-month grid (`2016-07 -> 2026-03`, `117` windows, `curated_only`, warmup `120`), proves identical summary-hash replay across two executions, shows that no bounded monthly window produces action-level divergence on this carrier, and ranks `2021-08` as the first exact monthly top-line-divergent subject (`+18.89` USD / `+0.1889` return points under `release_zero`) ahead of `2025-10` and `2018-03`
- the newest exact top-line-divergent anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_continuation_release_hysteresis_topline_subject_2026-05-04.md`, which turns the inventory-ranked `2021-08` winner into one dedicated exact subject (`tBTCUSD` / `3h` / `2021-08-01 -> 2021-08-31`, warmup `120`, `curated_only`), proves same-SHA repeat hashes across two runs, localizes the exact six-row continuation-release cluster on `2021-08-18 -> 2021-08-19`, and shows a bounded top-line improvement under `continuation_release_hysteresis = 0` (`+18.890086341649294` USD / `+0.18890086341649295` return points) with unchanged trade count and `action_diff_count = 0`
- the newest second positive exact-subject anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_continuation_release_hysteresis_topline_subject_2025_10_2026-05-04.md`, which turns the inventory-ranked `2025-10` follow-up into one dedicated exact subject (`tBTCUSD` / `3h` / `2025-10-01 -> 2025-10-31`, warmup `120`, `curated_only`), uses `genesis_backtest_verify` plus `python_engineering`, proves identical double-run hashes on the same dirty local evidence checkout (`evidence_checkout_clean = false`), records `runtime_base_sha` and `evidence_commit_sha` as distinct provenance fields that happened to match plus `carrier_sha256 = d6bfd4e18ea27f5cfd59f4ac1c6cff1f8762a1d35f6b5a369f5ccd004934913e`, localizes the thirteen-row baseline continuation-release cluster on `2025-10-17 -> 2025-10-18`, and shows a second bounded positive top-line improvement under `continuation_release_hysteresis = 0` (`+17.134840124999755` USD / `+0.17134840124999756` return points) with unchanged trade count and `action_diff_count = 0`
- the newest first opposite-sign exact-subject anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_continuation_release_hysteresis_topline_subject_2018_03_2026-05-04.md`, which fixes `2018-03` as the first opposite-sign exact subject from the frozen monthly inventory (`tBTCUSD` / `3h` / `2018-03-01 -> 2018-03-31`, warmup `120`, `curated_only`), records `Category: obs`, uses `genesis_backtest_verify` plus `python_engineering`, proves identical double-run hashes on the same dirty local evidence checkout (`evidence_checkout_clean = false`), records `runtime_base_sha` and `evidence_commit_sha` as distinct provenance fields that happened to match plus the same `carrier_sha256 = d6bfd4e18ea27f5cfd59f4ac1c6cff1f8762a1d35f6b5a369f5ccd004934913e`, localizes the twelve-row baseline continuation-release cluster on `2018-03-16 -> 2018-03-18`, and shows the first bounded negative top-line divergence under `continuation_release_hysteresis = 0` (`-0.9371672999986913` USD / `-0.009371672999986924` return points) with unchanged trade count and `action_diff_count = 0`
- the newest exact-subject synthesis anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_continuation_release_hysteresis_topline_subject_triad_synthesis_2026-05-04.md`, which closes the full-calendar-month top-line-divergent bench as a complete three-subject triad (`2021-08`, `2025-10`, `2018-03`): all three preserve unchanged trade count and `action_diff_count = 0`, all three share the same continuation-release policy/size-path mechanism, but the top-line sign is mixed (`+`, `+`, `-`), and the frozen monthly windows artifact contains no fourth `topline_changed = true` month on the current grid
- the only retained positive runtime slice in the recent chain is `docs/decisions/regime_intelligence/policy_router/ri_policy_router_bars7_continuation_persistence_runtime_closeout_2026-04-27.md`
- the aged-weak second-hit and aged-weak plus stability families are both closed negative / reverted, and the active-carrier handoff is now recorded in `docs/decisions/regime_intelligence/policy_router/ri_policy_router_aged_weak_active_carrier_truth_parked_handoff_2026-04-27.md`
- the bounded default-off RI-local runtime policy-router lane remains historically implemented and validated under `docs/decisions/regime_intelligence/policy_router/ri_policy_router_runtime_integration_packet_2026-04-23.md`, but it is not the active tuning lane today
- the tiny findings-bank hardening slice is now re-verified green on `HEAD` `e9500e1d`: live-checkout validator pass, packet-starter smoke, focused tests, and detached-worktree clean-checkout-like proof all passed without widening CI or `artifacts/**`
- the current local `defensive_probe` evidence anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_defensive_probe_exact_carrier_evidence_2026-04-29.md`, which localizes the line to one exact two-row selected-defensive pocket and keeps the concept non-authorizing for runtime/config/default follow-up
- the newest policy-improvement evidence anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_blocked_reason_split_2026-04-29.md`, which shows the blocked baseline-long divider is a mixed surface split across `stable_continuation_state` displacement plus the true suppressors `AGED_WEAK_CONTINUATION_GUARD` and `insufficient_evidence`
- the newest positive-year control anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_positive_year_insufficient_evidence_control_2026-04-29.md`, which pairs the March 2021 negative-year local window with a March 2025 positive-year control and shows that `insufficient_evidence` is a selective suppressor problem rather than a uniformly harmful or uniformly helpful one
- the newest selective feature-gate anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_selective_feature_gate_contrast_2026-04-30.md`, which keeps the comparison on the same blocked target shape and localizes the first candidate discriminator bundle to later regime age plus slightly stronger edge/confidence/clarity in the March 2021 cluster relative to the March 2025 control
- the newest displacement-crosscheck anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_discriminator_bundle_displacement_crosscheck_2026-04-30.md`, which shows that the candidate bundle mostly recurs as the same target-vs-displacement contrast in both years and therefore weakens standalone runtime-rule interpretation
- the newest displacement-normalized residual anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_displacement_normalized_residual_read_2026-04-30.md`, which closes the current four-cohort artifact reread with a bounded null: no clean sign-changing discriminator survives on this exact repo-visible surface once the generic target-vs-displacement pattern is accounted for
- the newest genuinely new bounded follow-up anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_2024_regression_pocket_isolation_2026-04-30.md`, which materializes one exact late-2024 low-zone regression pocket and records a locally weak mixed blocked cluster beside a healthier true `stable_continuation_state` displacement row and a still-weaker stable blocked context row; this remains observational only and does not expand runtime authority
- the newest bounded pocket-internal reread anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_2024_regression_pocket_reason_split_2026-04-30.md`, which keeps the same exact nine-row 2024 pocket fixed and shows that the clearest local weakness sits on the `insufficient_evidence` subset while `AGED_WEAK_CONTINUATION_GUARD` remains mixed rather than cleanly weak; this also remains observational only and does not expand runtime authority
- the newest bounded counterfactual-screen closeout anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_counterfactual_screen_2026-04-30.md`, which closes the exact 2024-vs-2020 `insufficient_evidence` screen with a bounded null: no clean counterfactual unlock screen survives on the chosen `fwd_16` proxy surface, and `bars_since_regime_change <= 282` remains descriptive only rather than runtime or concept authority
- the newest bounded truth-surface-correction anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_truth_surface_correction_2026-04-30.md`, which replaces the weak `2024-11` target with an exact non-March July `2024` subset, restores a genuinely truth-opposed `2024`-vs-`2020` bounded surface on the chosen offline `fwd_16` proxy, and records one surviving single-field screen (`bars_since_regime_change <= 166`) that should still be read as an exact-subject envelope separator rather than runtime authority
- the newest bounded within-envelope falsifier anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_july_2024_within_envelope_falsifier_2026-05-04.md`, which keeps the exact July `2024` / frozen `2020` surface fixed, proves the inherited single-field `bars_since_regime_change <= 166` screen is still envelope-only on the July window by itself, but records one bounded two-field within-envelope survivor when that age ceiling is paired with low `action_edge` / `confidence_gate` / `clarity_score` ceilings; this remains observational only and does not expand runtime authority
- the newest bounded late-2024 portability anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_late_2024_recurrence_falsifier_2026-05-04.md`, which keeps one exact late-2024 `5 + 6` surface fixed, shows that all three transported July rule variants fail fully on that subject (`0 / 5` target, `0 / 6` anti-target), but also shows that the weak-signal ceilings recur descriptively there (`clarity_score <= 37` selects `5 / 5` target and `1 / 6` anti-target, while `action_edge <= 0.034334`, `confidence_gate <= 0.517167`, and the three-way weak-signal intersection each select `4 / 5` target and `0 / 6` anti-target); this remains observational only and keeps the July conjunction exact-surface rather than portable authority
- the newest bounded second-holdout anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_2022_06_weak_signal_holdout_2026-05-04.md`, which keeps one exact `2022-06` `5 + 4` surface fixed, shows that all three transported July rule variants fail fully again on that subject (`0 / 5` target, `0 / 4` anti-target) because every row sits above the July age ceiling, and shows that the weak-signal family recurs descriptively there even more cleanly (`action_edge <= 0.034334`, `confidence_gate <= 0.517167`, `clarity_score <= 37`, and the three-way intersection each select `5 / 5` target and `0 / 4` anti-target); this remains observational only and does not restore transport authority
- the newest insufficient-evidence parking anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_translation_parking_synthesis_2026-05-04.md`, which parks only this bounded translation chain: the July conjunction remains exact-surface evidence, unchanged transport is now falsified on both late-2024 and `2022-06`, and the weak-signal family remains descriptive only rather than runtime or concept authority
- the newest insufficient-evidence D1 exact-subject-pair anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_exact_subject_pair_2019_06_vs_2022_06_2026-05-04.md`, which reuses exact `2022-06` only as a fixed weak-control subject for a new D1 question, proves the exact `2019-06` harmful target and exact `2022-06` weak-control target remain polarity-opposed on the chosen offline proxy, and shows that three direct non-age decision-time fields (`action_edge >= 0.027801`, `clarity_raw >= 0.361280`, `confidence_gate >= 0.513901`) each separate the exact target/control pair perfectly while still leaking across the fixed sibling/context rows; this remains observational only and does not restore transport authority or runtime authority
- the newest insufficient-evidence D1 family-survival anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_family_survival_2019_06_vs_2025_03_2026-05-04.md`, which keeps the exact `2019-06` harmful target fixed and swaps the control side to the exact `2025-03` weak-control target, proves the same three direct non-age decision-time fields (`action_edge >= 0.027801`, `clarity_raw >= 0.361280`, `confidence_gate >= 0.513901`) survive unchanged on a second exact pair, records identical double-run artifact hashes (`81D8B30B4EA25814B58EB37443B74EC9E5E5DA68D1020167EC9E6FE9983A68F7`), shows that the descriptive age-only comparator collapses on this pair (`bars_since_regime_change <= 164` selects both target cohorts), and shows that the surviving non-age family still selects every fixed sibling/context row; this remains second-pair recurrence evidence only and does not restore transport authority, classifier-clean authority, or runtime/promotion authority
- the newest insufficient-evidence D1 boundary-gap anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_boundary_gap_to_local_context_floor_2026-05-04.md`, which rereads only the two existing D1 evaluation artifacts, records identical double-run artifact hashes (`E726FEE746F77276B8E0B5B17BABDD9C7227AF33940115463DFB3838AE35CCF1`), and shows that on the exact `2019-06` / `2022-06` / `2025-03` tri-surface the harmful target sits materially closer to its local context floor than both weak-control target sets on `action_edge`, `confidence_gate`, and `clarity_raw`; this remains exact-surface-only research evidence and does not restore transport or runtime authority
- the newest insufficient-evidence D1 third-control transport anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_boundary_gap_transport_check_2019_06_vs_2020_10_11_2026-05-04.md`, which keeps the exact `2019-06` harmful anchor fixed, reuses the exact `2020-10/11` weak-control cluster only as one source-backed third control surface, proves that the locked raw `2020` action-diff rows do carry direct `clarity_raw` on all required target/context rows, and shows that the same boundary-gap ordering survives with non-overlapping harmful-versus-control ranges on `action_edge`, `confidence_gate`, and `clarity_raw`; this remains exact-surface-only research evidence and does not restore runtime, policy, or promotion authority
- the newest insufficient-evidence D1 synthesis anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_four_surface_synthesis_2026-05-05.md`, which closes the current four-surface D1 chain as one bounded docs-only re-anchor: exact `2019-06` harmful anchor plus exact `2022-06`, exact `2025-03`, and source-backed exact `2020-10/11` weak-control surfaces, all supporting the same non-age family on `action_edge`, `confidence_gate`, and `clarity_raw`; this sharpens the current read to exact-surface envelope-relative recurrence, but still keeps the line observational only, context-leaky, and non-authoritative
- the newest insufficient-evidence D1 context-clean falsifier anchor is now `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_context_clean_selectivity_falsifier_2026-05-05.md`, which keeps that exact four-surface bank fixed, rereads it only as combined target-bank ceiling versus combined fixed-context minimum, and shows that the same admitted non-age family remains context-clean on the frozen bank (`19` claim-bearing targets, `14` fixed context rows, no context leaks on `action_edge`, `confidence_gate`, or `clarity_raw`); this remains exact-bank-only, observational only, and non-authoritative rather than runtime, transport, or promotion authority
- the Legacy-surface separation anchor is now `docs/decisions/legacy/policy_router/legacy_policy_router_surface_separation_precode_packet_2026-04-30.md`, which freezes the rule that current RI `enabled` vs `absent` evidence must not be relabeled as Legacy and that any future true Legacy line must start with separate Legacy-only namespaces plus explicit identity assertions
- the first Legacy-2024 setup anchor is now `docs/decisions/legacy/policy_router/legacy_policy_router_2024_control_setup_precode_packet_2026-04-30.md`, which freezes the next honest Legacy question as a setup proof: before any true Legacy `2024` control run is proposed, the repo must prove that a router-leaf carrier can stay `strategy_family = legacy`, keep `authority_mode = legacy`, and avoid RI signature drift
- the first Legacy-2024 setup implementation anchor is now `docs/decisions/legacy/policy_router/legacy_policy_router_2024_control_setup_implementation_packet_2026-04-30.md`, which materializes one tmp-only Legacy carrier at `tmp/policy_router_evidence/legacy/tBTCUSD_3h_legacy_policy_router_2024_setup_carrier_20260430.json` and locks a focused governance proof that the enabled router leaf survives canonical validation while the carrier remains true Legacy
- any future reopen on the aged-weak surface must start with fresh docs-only evidence anchored to the active carrier truth rather than reuse of the falsified `2023-12-30 21:00` residual-row premise
- this file is only a drift anchor; detailed closeout logic belongs in the governance notes above rather than here

## Explicitly not active by default

Unless the user reopens them explicitly with the needed authority, do **not** treat these as active:

- the earlier `3h` historical validation lane from the prior working anchor
- inherited runtime/integration authority from RI research docs
- runtime-default changes
- paper-shadow follow-up fixes
- promotion/champion claims from isolated research evidence

## Key anchors already verified

- `docs/governance/concept_evidence_runtime_lane_model_2026-04-23.md` is the canonical practical definition of concept / research-evidence / runtime-integration lanes
- `.github/copilot-instructions.md` already says to prefer the cheapest admissible lane before proposing durable runtime structure
- `docs/OPUS_46_GOVERNANCE.md` treats lane classification as workflow framing, not new authority
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_evidence_slice_precode_packet_2026-04-23.md` freezes the first fresh RI router replay subject without granting execution authority
- `scripts/analyze/scpe_ri_v1_router_replay.py` and `results/research/scpe_v1_ri/` exist as tracked historical reference surfaces, not inherited authority for the fresh slice
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_counterfactual_closeout_report_2026-04-23.md` closes the bounded counterfactual lane and records the current blocker ordering without granting semantics or runtime approval
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_semantics_packet_2026-04-23.md` opens the next bounded semantics question without authorizing execution or runtime follow-up by itself
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_backtest_precode_packet_2026-04-23.md` defines the first exact bounded backtest subject for the mandate-2 candidate without authorizing execution by itself
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_backtest_setup_only_packet_2026-04-23.md` defines the setup-only backtest surface and records that the candidate run still lacks a bounded repo-visible carrier
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_candidate_carrier_precode_packet_2026-04-23.md` narrows the candidate-carrier hypothesis to the transition-state propagation seam and treats `risk_state.py` as downstream sizing evidence, not as the carrier itself
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_candidate_carrier_implementation_packet_2026-04-23.md` defines the smallest high-sensitivity code-slice contract and its required Full-path gates before any code may begin
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_candidate_gate_carrier_precode_packet_2026-04-23.md` re-packets the next honest widened carrier hypothesis around `decision_gates.py::select_candidate(...)` and explicit config-authority support if a new default-off research leaf is required
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_candidate_gate_carrier_implementation_packet_2026-04-23.md` defines the implemented bounded carrier slice for `decision_gates.py` plus explicit config-authority support and default-off parity/backcompat proof
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_bridge_activation_precode_packet_2026-04-23.md` defines the next docs-only future config slice that would make the new carrier expressible as a separate candidate bridge artifact without reopening launch authority
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_bridge_activation_implementation_packet_2026-04-23.md` defines the completed config-only candidate-artifact creation slice for the defensive-transition bridge path while keeping baseline and launch surfaces separate
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_backtest_launch_boundary_packet_2026-04-23.md` now defines the exact paired baseline/candidate no-save command targets for the explicit suppressed-write surface that sets `GENESIS_PRECOMPUTE_CACHE_WRITE=0`
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_backtest_launch_authorization_packet_2026-04-23.md` now records `AUTHORIZED NOW` for the exact paired pre-execution surface only when that explicit suppressed-write command shape is preserved
- `docs/analysis/regime_intelligence/router_replay/ri_router_replay_defensive_transition_backtest_execution_summary_2026-04-23.md` now records the completed bounded paired execution and the observed no-divergence outcome on the exact authorized surface
- `docs/decisions/regime_intelligence/router_replay/ri_router_replay_defensive_transition_backtest_precompute_containment_implementation_packet_2026-04-23.md` defines the bounded high-sensitivity runtime slice that introduces an explicit opt-in suppression path for precompute disk writes without changing the default canonical path
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_runtime_integration_packet_2026-04-23.md` now defines the bounded default-off runtime slice for RI-local policy switching, dwell/hysteresis state carry, and explicit return-to-continuation semantics below family/champion authority
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_weak_pre_aged_single_veto_release_candidate_packet_2026-04-27.md` now preserves the next seam-A-only follow-up candidate after the cooldown-displacement diagnosis without granting runtime authority by itself
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_weak_pre_aged_single_veto_release_implementation_packet_2026-04-27.md` now records the approved-with-notes runtime contract for the single-veto seam-A follow-up inside the exact bounded seam-A scope
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_weak_pre_aged_single_veto_release_failset_evidence_2026-04-27.md` now records the first paired December fail-set evidence pass for the single-veto seam-A candidate, including the now-removed two-bar displacement loop and the remaining residual blocked-long set
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_weak_pre_aged_single_veto_release_residual_blocked_longs_diagnosis_2026-04-27.md` now records that the remaining residual blocked-long set is split across raw `insufficient_evidence` / no-trade-floor behavior and the older `AGED_WEAK_CONTINUATION_GUARD` seam, rather than one remaining seam-A issue
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_weak_pre_aged_single_veto_release_low_zone_insufficient_evidence_diagnosis_2026-04-27.md` now records that the chosen low-zone residual rows fail `confidence_floor` and `edge_floor` while clarity remains healthy, making them an evidence-floor surface rather than a seam-A latch surface
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_low_zone_near_floor_insufficient_evidence_residual_hypothesis_packet_2026-04-27.md` now preserves the next docs-only low-zone follow-up candidate as one bounded near-floor insufficient-evidence residual hypothesis, not as a runtime-authoritative threshold carve-out
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_low_zone_near_floor_insufficient_evidence_downstream_handoff_implementation_packet_2026-04-27.md` now records the attempted enabled-only runtime handoff packet, but its first implementation attempt was reverted after focused fail-B verification invalidated the packet's locked residual-envelope assumptions
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_low_zone_bars8_evidence_floor_candidate_packet_2026-04-27.md` now preserves the next docs-only low-zone candidate on the exact two-row post-filter bars-8 evidence-floor surface while explicitly keeping the blocked three-row packet non-authoritative
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_bars7_continuation_persistence_candidate_packet_2026-04-27.md` now preserves the separate bars-7 continuation-persistence seam that was previously bundled inside the low-zone family
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_bars7_continuation_persistence_runtime_packet_2026-04-27.md` now records the implemented bounded bars-7 runtime slice, its exact helper-hit proof, and its green gate bundle
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_bars7_continuation_persistence_runtime_closeout_2026-04-27.md` now closes that bars-7 slice positive and hands the remaining work to the aged-weak residual seam only
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_aged_weak_second_hit_release_candidate_packet_2026-04-27.md` now preserves the next docs-only aged-weak residual candidate on the exact `2023-12-28 09:00` / `2023-12-30 21:00` row-set
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_aged_weak_second_hit_release_runtime_packet_2026-04-27.md` now serves as the historical bounded runtime packet for that aged-weak residual seam, and `docs/decisions/regime_intelligence/policy_router/ri_policy_router_aged_weak_second_hit_release_runtime_closeout_2026-04-27.md` records its negative feasibility verdict and revert
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_enabled_vs_absent_annual_evidence_2026-04-28.md` records the frozen annual verdict as mixed and already frames `2024` regression-pocket isolation as the next honest read-only follow-up if the line is reopened
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_enabled_vs_absent_curated_annual_evidence_2026-04-28.md` now extends that annual picture onto the broader curated `2016-06-07 .. 2026-04-15` surface and records the full-year split: positive `2018/2020/2022/2025`, negative `2019/2021/2024`, mixed `2017/2023`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_negative_year_pocket_isolation_2026-04-28.md` now records the first read-only negative-pocket summary and shows that the clearly negative curated years are dominated by late low-zone `LONG -> NONE` suppression plus later `NONE -> LONG` continuation displacement rather than by a dominant early defensive pocket
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_positive_vs_negative_pocket_comparison_2026-04-28.md` now compares that pocket shape against clearly positive curated years and records that the broad suppression+continuation pattern is shared rather than unique to negative years
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_shared_pocket_outcome_quality_2026-04-28.md` now adds the first timestamp-close observational outcome proxy pass and says the clearest year-group split appears on the blocked baseline-long cohort rather than on a clean continuation-substitution superiority story
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_substituted_continuation_tail_dispersion_2026-04-28.md` now localizes the continuation cohort by year and shows that continuation quality is itself year-heterogeneous: `2020` is clearly continuation-friendly, while `2018`, `2022`, and `2025` are materially weaker on the same descriptive proxy surface
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_substituted_continuation_local_tail_windows_2026-04-28.md` now adds a fixed `<=24h` descriptive window view and shows that `2020` concentrates its bad continuation rows into a few acute windows, while `2018` and `2022` revisit continuation-hostile multi-row windows repeatedly across the year and `2025` is weaker in a more fragmented way
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_blocked_vs_substituted_same_windows_2026-04-28.md` now compares the two cohorts inside two pre-fixed mixed local windows and shows that the local blocked-vs-substituted balance flips across the windows: blocked rows look locally stronger in the mixed March 2020 union window but locally weaker in the recurrent hostile March 2018 window, which means the same-window surface is phase-sensitive rather than a universal cohort-ranking rule
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_blocked_vs_substituted_same_window_phase_ordering_2026-04-29.md` now rereads those same two fixed windows as explicit chronology: `2020` is substituted-early / blocked-later with a mixed rebound and blocked terminal timestamp, while `2018` is blocked-early / substituted-middle / blocked-relapse / substituted-late, so the same-window sign flip is best read as segment occupancy and handoff timing rather than a universal cohort winner
- the latest RI local-window findings are now also registered in the repo-native findings bank as `FIND-2026-0006` (`substituted_continuation_local_window_shape`) and `FIND-2026-0007` (`blocked_vs_substituted_same_window_phase_ordering`), and the narrow findings-bank persistence surface is now git-trackable on this checkout through the findings bundles, their exact ArtifactRecords, the findings schema, and `artifacts/research_ledger/indexes/findings_index.json`; these remain research-only `direction_lock` findings rather than runtime or promotion authority
- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_defensive_probe_concept_precode_packet_2026-04-29.md` now defines the bounded concept-only question for whether the raw `defensive_transition_state` substrate is coherent enough to warrant a future `defensive_probe` candidate; it explicitly forbids treating that candidate as already implemented or as runtime authority
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_defensive_probe_exact_carrier_evidence_2026-04-29.md` now localizes that concept line to the smallest honest carrier on current repo-visible evidence: one two-row selected-defensive pocket containing the lone baseline `defensive_transition_state` selected-defensive row plus its stronger `transition_pressure_detected` sibling
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_blocked_reason_split_2026-04-29.md` now splits the blocked baseline-long cohort by reason and shows that the annual divider is not purely one suppressor seam: `stable_continuation_state` is the largest blocked bucket, while `insufficient_evidence` shows the strongest negative-vs-positive gap among the true suppressors
- `registry/schemas/strategy_candidate_manifest.schema.json` defines the closed archival schema for strategy-candidate lifecycle metadata only
- `registry/manifests/strategy_candidates.dev.json` seeds the first preserved RI candidate identities without creating promotion or runtime authority
- `config/strategy/candidates/README.md` now documents the candidate-bank lifecycle manifest and explicitly keeps `promotion_compare`, `champion_freeze`, `readiness`, `cutover`, and live-near authority out of ordinary runtime selection semantics

## Last verified facts relevant to today

- the fresh pre-code RI replay packet already freezes the allowed future input/output envelope and explicit non-inheritance rule
- the frozen Phase C evidence inputs exist locally under `results/research/ri_advisory_environment_fit/phase3_phasec_evidence_capture_v2_2026-04-17/`
- the historical SCPE replay script and approved eight-file replay root exist locally as comparison/reference context only
- the baseline fresh replay surface still shows raw defensive `30` versus selected defensive `2`
- the smallest honest `defensive_probe` carrier on current repo-visible evidence is the two-row selected-defensive pocket at `2024-01-16T00:00:00` / `2024-01-17T12:00:00`, where the lone `defensive_transition_state` selected-defensive row sits beside the stronger `transition_pressure_detected` row and the nearest retained no-trade comparator does not begin until `21` bars since regime change
- the blocked baseline-long annual divider is now proven to be mixed rather than single-seam: `stable_continuation_state` is the largest blocked bucket in both positive and negative year groups, so the action-layer blocked cohort still contains major continuation-displacement / timing mass
- among the true suppressors inside that blocked cohort, `insufficient_evidence` now shows a stronger negative-vs-positive `fwd_16` gap than `AGED_WEAK_CONTINUATION_GUARD`, while `switch_blocked_by_min_dwell` does not look like the leading annual blocker on this surface
- the completed March 2021 negative-year local window plus the new March 2025 positive-year control now show a real selectivity problem on `insufficient_evidence`: the 2021 target cluster was locally favorable while the 2025 control cluster is locally weak and sits beside materially stronger nearby displacement rows, so the next honest question is discriminative selectivity rather than blanket weakening/strengthening
- the fixed 2021-vs-2025 target contrast now shows that those two windows share the same blocked low-zone `LONG -> NONE` `RI_no_trade_policy` shape, and that the strongest descriptive separator on current repo-visible evidence is later regime age in 2021 (`bars_since_regime_change` mean gap `+6.75`) alongside modestly stronger `action_edge`, `confidence_gate`, and `clarity`; `dwell_duration` moves the other way and does not by itself justify a rule
- the fixed four-cohort displacement crosscheck now shows that the same bundle mostly recurs inside both years against the already-fixed nearby displacement rows (`4 / 2 / 5 / 2` row lock), so the current bundle reads more like a generic blocked-target-vs-displacement contrast than a clean harmful-year-only runtime discriminator; any residual magnitude differences remain descriptive only
- the bounded displacement-normalized residual reread now closes the current March 2021/March 2025 four-cohort artifact loop: every candidate bundle field keeps the same sign after target-minus-displacement normalization in both years, so the remaining residuals are magnitude skews only and do not yield a clean runtime-rule candidate on the current repo-visible surface
- the fixed 2024 regression-pocket isolation now records one genuinely new bounded negative-year pocket on the same late low-zone family: the exact mixed blocked cluster (`9` rows, reason split `4/5`) is locally weak on `fwd_16`, the nearby true `stable_continuation_state` displacement row is locally healthier, and the nearby stable blocked context row is weaker still, so the pocket reads as a compact suppression/displacement blend rather than a pure single-reason seam; the local JSON artifact for this slice remains ignored unless explicitly staged
- the fixed 2024 regression-pocket reason split now sharpens that pocket locally: the exact `insufficient_evidence` subset (`5` rows) is uniformly weak on `fwd_8`/`fwd_16` and weaker on local edge/confidence/clarity than the exact `AGED_WEAK_CONTINUATION_GUARD` subset (`4` rows), while the aged-weak side remains mixed rather than cleanly strong or cleanly weak; this still does not authorize runtime changes from local evidence alone
- the completed exact 2024-vs-2020 counterfactual-screen slice now closes fail-closed at `no_surviving_screen`: on the chosen offline `fwd_16` proxy both the fixed 2024 target and the fixed 2020 control remain locally weak, so the intended harmful-vs-correct polarity is absent on that exact surface and the strongest surviving field (`bars_since_regime_change <= 282`) must be treated as a descriptive envelope separator only
- the completed truth-surface-correction slice now shows that the earlier null was limited by target-truth selection rather than by a universal lack of bounded separators: the exact July `2024` corrected target is strongly favorable on the same offline `fwd_16` proxy while the frozen `2020` control remains weak, and the corrected exact pair yields a surviving single-field screen (`bars_since_regime_change <= 166`) that cleanly separates the full July `2024` envelope (`bars 164..166`) from the frozen `2020` weak-control family (`bars 302..304`) without isolating only the target rows inside July `2024`
- the completed July `2024` within-envelope falsifier now tightens that read on the same exact surface: the inherited single-field survivor remains envelope-only, but a bounded two-field refinement survives when the age ceiling is paired with a weak-signal ceiling (`action_edge <= 0.034334`, `confidence_gate <= 0.517167`, or `clarity_score <= 37`), which means the current surviving evidence is a bounded conjunction rather than age-only authority and still remains observational only
- the completed late-2024 recurrence falsifier now tightens portability on the same line: all three transported July rule variants fail completely on the exact late-2024 `insufficient_evidence` subject (`0 / 5` target, `0 / 6` anti-target), while the weak-signal ceilings recur descriptively only (`clarity_score <= 37` selects `5 / 5` target and `1 / 6` anti-target; `action_edge <= 0.034334`, `confidence_gate <= 0.517167`, and the three-way weak-signal intersection each select `4 / 5` target and `0 / 6` anti-target), so the July conjunction remains exact-surface evidence rather than portable authority
- the completed `2022-06` weak-signal holdout now closes the second fixed non-March pressure test on the same line: all three transported July rule variants fail again on the exact `2022-06` `5 + 4` surface (`0 / 5` target, `0 / 4` anti-target) because every row sits above the July age ceiling, while the weak-signal field family recurs descriptively with full separation (`5 / 5` target, `0 / 4` anti-target on `action_edge`, `confidence_gate`, `clarity_score`, and their three-way intersection); this remains observational only and does not restore transport authority
- the bounded insufficient-evidence translation chain is now parked by `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_translation_parking_synthesis_2026-05-04.md`: the July conjunction stays exact-surface only, unchanged transport is falsified on both late-2024 and `2022-06`, the weak-signal family remains descriptive only, and no runtime/config/promotion authority is granted from this parked chain
- the completed D1 exact subject-pair slice now answers the next honest bounded post-parking question on one fresh harmful/control pair: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_exact_subject_pair_2019_06_vs_2022_06_2026-05-04.md` shows that the fixed `2019-06` harmful target and fixed `2022-06` weak-control target are separable by more than age alone on current repo-visible evidence (`action_edge`, `clarity_raw`, and `confidence_gate` each give full `5 / 5` versus `0 / 5` target/control separation), but that the same non-age rules also select the fixed sibling/context rows, so the current answer is pair-local research evidence rather than a clean broader local-context classifier or any runtime/promotion authority
- the completed D1 second-pair family-survival slice now answers the next honest bounded recurrence question on a fresh weak-control subject: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_family_survival_2019_06_vs_2025_03_2026-05-04.md` shows that the same non-age rules survive unchanged on exact `2019-06` versus exact `2025-03`, that `clarity_raw` remains fully evaluable on the locked target rows, that the age-only comparator does not carry the result, and that the surviving family still selects every fixed sibling/context row; the current answer is therefore second-pair recurrence but still context-leaky research evidence rather than clean classifier or runtime/promotion authority
- the completed D1 boundary-gap slice now closes the bounded residual/context-leak question on the fixed tri-surface with a bounded yes: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_boundary_gap_to_local_context_floor_2026-05-04.md` shows that the harmful-target boundary-gap ranges are non-overlapping and strictly smaller than both exact weak-control target gap ranges on `action_edge`, `confidence_gate`, and `clarity_raw`, while `clarity_score` remains descriptive only; this sharpens the current recurring D1 family into envelope-relative research evidence only and does not restore transport or runtime authority
- the completed D1 third-control transport slice now closes the `2020` admission/recurrence question with a bounded non-null result: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_boundary_gap_transport_check_2019_06_vs_2020_10_11_2026-05-04.md` shows that the exact locked raw `2020` target/context rows do carry direct `clarity_raw`, so the slice does not fail closed on field admission, and that the same boundary-gap ordering survives on exact `2019-06` versus exact `2020-10/11` with non-overlapping harmful-versus-control target gap ranges on `action_edge`, `confidence_gate`, and `clarity_raw`; this remains third-control recurrence evidence only and does not restore runtime, policy, or promotion authority
- the completed D1 four-surface synthesis slice now re-anchors the lane after the `2020` transport result: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_four_surface_synthesis_2026-05-05.md` says the current line is now best read as a bounded four-surface exact-surface recurrence bank on `action_edge`, `confidence_gate`, and `clarity_raw`, with the `2020` field-admission uncertainty closed, but also says just as explicitly that the line remains observational only, context-leaky, non-authoritative, and not a portable runtime or transport-clean rule
- the completed D1 context-clean falsifier slice now answers the first fresh post-synthesis bank-reread question with a bounded non-null result: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_context_clean_selectivity_falsifier_2026-05-05.md` shows that on the exact frozen four-surface D1 bank the combined target-bank ceiling stays strictly below the combined fixed-context minimum on `action_edge`, `confidence_gate`, and `clarity_raw`, with the tightest bound still coming from the harmful-side sibling row rather than a control context row; this sharpens the current bank-level read but remains exact-bank-only, observational only, and non-authoritative rather than runtime, transport, or promotion authority
- the current annual `enabled` vs `absent` probe is not a Legacy comparison: its carrier remains `strategy_family = ri` with `authority_mode = regime_module`, and the `absent` branch removes only `multi_timeframe.research_policy_router`; any future Legacy line must therefore use separate Legacy-only docs/results/tmp namespaces and explicit identity assertions per `docs/decisions/legacy/policy_router/legacy_policy_router_surface_separation_precode_packet_2026-04-30.md`
- current repo-visible config seams keep the Legacy question open but not yet authorized: `RuntimeConfig` and config-authority whitelist `multi_timeframe.research_policy_router`, while `family_registry.py` still requires a true Legacy carrier to keep `authority_mode != regime_module` and avoid RI signature markers; therefore the first Legacy `2024` slice is a setup/control proof per `docs/decisions/legacy/policy_router/legacy_policy_router_2024_control_setup_precode_packet_2026-04-30.md`, not an already-runnable backtest subject
- the setup-proof gap for that first Legacy `2024` control line is now closed on the config-admission surface: the tmp carrier `tmp/policy_router_evidence/legacy/tBTCUSD_3h_legacy_policy_router_2024_setup_carrier_20260430.json` validates through `ConfigAuthority`, preserves the enabled router leaf canonically, keeps `authority_mode = legacy`, and stays free of RI signature markers under the focused governance proof in `tests/governance/test_legacy_policy_router_setup.py`; this still does not authorize a runnable Legacy backtest by itself
- `switch_threshold: 2 -> 1` and `defensive_transition_state mandate/confidence: 1 -> 2` are behavior-equivalent on the current frozen surface
- `min_dwell: 3 -> 1` increases continuation dominance rather than improving defensive separation
- `hysteresis: 1 -> 0` is a no-op on the baseline surface, and the apparent hysteresis blocker reduces to raw mandate `1` versus continuation mandate `2/3`
- the first bounded backtest subject is now fixed to `config/strategy/candidates/3h/tBTCUSD_3h_slice8_runtime_bridge_20260326.json` on `tBTCUSD`, `3h`, `2024-01-02 -> 2024-12-31`, warmup `120`, canonical 1/1 mode, and seed `42`
- `scripts/run/run_backtest.py` plus the fixed bridge config can express the baseline run on current repo-visible surfaces, but do not expose a dedicated config/CLI carrier for `defensive_transition_state mandate/confidence 2`
- current backtest result saving remains timestamp-driven under `results/backtests/` and `results/trades/`, so exact materialized filenames remain a launch-time review item rather than a pre-fixed CLI surface
- `src/core/strategy/decision_sizing.py` is the currently visible seam that propagates `last_regime` / `bars_since_regime_change`
- `src/core/intelligence/regime/risk_state.py` consumes that state for `transition_mult` sizing only and does not, on current repo-visible evidence, provide the missing mandate carrier by itself
- the bounded candidate-gate carrier slice has now been implemented, passed its required gates, and cleared post-diff audit on the current branch
- `src/core/strategy/decision_gates.py::select_candidate(...)` now exposes one explicit default-off research path keyed by `multi_timeframe.research_defensive_transition_override`
- `src/core/config/schema.py` now canonicalizes the leaf away when absent or disabled, and `src/core/config/authority.py` explicitly whitelists only `enabled`, `guard_bars`, and `max_probability_gap`
- `tests/utils/test_decision_gates_contract.py`, `tests/utils/test_decision_scenario_behavior.py`, `tests/governance/test_config_schema_backcompat.py`, and `tests/integration/test_golden_trace_runtime_semantics.py` now prove enabled-path behavior plus default-off parity/backcompat for the leaf
- the fixed bridge config `config/strategy/candidates/3h/tBTCUSD_3h_slice8_runtime_bridge_20260326.json` remains unchanged on the current branch
- the separate candidate bridge artifact `config/strategy/candidates/3h/tBTCUSD_3h_slice8_runtime_bridge_defensive_transition_20260423.json` now exists and validates through both `ConfigAuthority().validate(...)` and the current `run_backtest.py` config-file merge path
- the config-only bridge-artifact slice passed targeted pre-commit validation, explicit baseline immutability proof, `tests/governance/test_config_schema_backcompat.py`, config-authority lifecycle selectors, determinism replay, feature-cache invariance, and pipeline invariant selectors
- the `config_authority_lifecycle_check` skill is now evidenced for this slice by the green lifecycle selectors listed above
- `scripts/run/run_backtest.py` currently exposes the paired no-save boundary surfaces needed for the exact subject: `--config-file`, `--warmup`, `--data-source-policy`, `--fast-window`, `--precompute-features`, `--decision-rows-out`, `--decision-rows-format`, and `--no-save`
- the paired launch-boundary packet now fixes the exact explicit suppressed-write command surface by adding `GENESIS_PRECOMPUTE_CACHE_WRITE=0` to the paired target shape while keeping outputs bounded to `results/backtests/ri_router_defensive_transition_backtest_20260423/baseline_decision_rows.ndjson`, `results/backtests/ri_router_defensive_transition_backtest_20260423/candidate_decision_rows.ndjson`, and a later human-written execution summary
- `src/core/backtest/engine.py` now supports one explicit suppression path: when `GENESIS_PRECOMPUTE_CACHE_WRITE=0`, canonical precompute may still read an existing cache entry and build in-memory precomputed features, but it does not create `cache/precomputed/` and does not attempt `_np.savez_compressed(...)` on cache miss
- the launch-authorization packet now records that the explicit suppressed-write surface is authorized for pre-execution, but that authorization does not imply execution already happened
- the bounded paired execution is now complete on `HEAD` `efdce46b`, and the emitted baseline/candidate decision-row files are bit-for-bit identical (`D75471906D9C82E981B51D7281F41A7F1CA71DC00779751C707872CEECF9D804`, `2793` lines each)
- top-line stdout metrics were also identical across the pair (`6.40%` return, `118` trades, `58.5%` win rate, `0.261` Sharpe, `1.43%` max drawdown, `2.27` profit factor, `0.3567` score)
- the frozen annual enabled-vs-absent note is now explicit that `2024` is the negative annual year and that the next honest follow-up is a read-only `2024` regression-pocket isolation slice rather than fresh tuning
- the curated annual enabled-vs-absent note now broadens that picture: the router leaf is materially active across `2016-06-07 .. 2026-04-15`, but full-year benefit remains mixed with negative years `2019`, `2021`, and `2024`, so any reopen should isolate those negative pockets before proposing runtime work
- the first negative-pocket isolation note now says the negative full years are dominated by late low-zone suppression plus later continuation displacement, but it does not yet prove that this shape is unique to negative years without comparison against clearly positive years
- the new positive-vs-negative pocket comparison note now says that the broad suppression+continuation shape is shared across both clearly negative and clearly positive curated years, so the next honest read-only question is outcome quality inside the shared shape rather than pocket presence alone
- the new shared-pocket outcome-quality proxy note now says the first descriptive signal is stronger on the blocked baseline-long cohort: blocked longs look directionally weaker in the positive year group than in the negative year group, while the substituted continuation cohort does not yet separate cleanly on the same proxy surface
- the new substituted-continuation tail-dispersion note now says the continuation cohort does not align cleanly with the annual positive-vs-negative split either: `2020` is continuation-friendly, `2018` is heavy-left-tail skewed, and `2022/2025` are broadly weak on the current proxy surface, which further strengthens the blocked-baseline cohort as the clearer annual divider
- the new local-tail-window note now sharpens that story again: `2020` still has severe continuation-tail windows, but mostly as a few acute concentrations, whereas `2018` and `2022` show recurrent multi-row continuation-hostile windows across the year and `2025` looks weaker in a more fragmented form; annual group labels remain too coarse for continuation interpretation
- the new same-window note now shows that even inside fixed mixed local windows there is no single blocked-versus-substituted winner: the March 2020 mixed window is continuation-left-tail-loaded early and blocked-stronger later, while the March 2018 recurrent hostile window points the other way, so local comparison is best treated as phase-ordering evidence rather than universal cohort ranking
- the two newest RI findings now sit on the narrow tracked findings-bank persistence surface and remain discoverable by preflight lookup, while the rest of `artifacts/` stays ignored; this resolves the earlier `.gitignore` mismatch without widening runtime, promotion, or general generated-artifact semantics
- `scripts/preflight/findings_preflight_lookup.py` now also exposes an explicit `--validate-index-projection` mode that derives the expected `artifacts/research_ledger/indexes/findings_index.json` from the committed findings bundles plus their ArtifactRecords and fails loudly when the local materialized index is missing or diverges; default lookup behavior remains unchanged and packet-starter import/usage still passes on the current checkout
- on `feature/next-slice-2026-04-29` at `HEAD` `3d8bfcaf`, both the live checkout and a detached temporary worktree pass `scripts/preflight/findings_preflight_lookup.py --validate-index-projection`, packet-starter smoke, and the focused preflight test selectors, so the findings-bank validator is re-verified on a clean-checkout-like surface without relying on the earlier archived proof folder
- the first repo-native candidate lifecycle registry slice has now passed focused registry validation, import smoke, determinism smoke, feature-cache invariance selectors, and pipeline fast-hash guard selectors without widening runtime/default/champion authority
- `src/core/governance/registry.py` now validates optional `strategy_candidates*.json` manifests additively, including duplicate candidate-id checks, duplicate `config_path` checks, and existence checks for referenced candidate configs
- the current closed schema intentionally allows only `research`, `candidate`, `promotion_compare`, and `champion_freeze`, and fences `config_path` to `config/strategy/candidates/**/*.json`
- the bounded policy-router implementation lane remains RESEARCH-valid so long as it stays inside `ri_policy_router.py`, `decision.py`, `schema.py`, `authority.py`, and the exact test/doc surfaces named in the packet, with absent-vs-disabled parity proven and no drift into `family_registry.py`, `family_admission.py`, `decision_gates.py`, or `decision_sizing.py`
- `src/core/strategy/ri_policy_router.py` now supports an enabled-only `continuation_release_hysteresis` override that defaults to shared `hysteresis` and only changes the exact `POLICY_DEFENSIVE -> POLICY_CONTINUATION` release seam; `src/core/config/schema.py` elides absent/equal values canonically, `src/core/config/authority.py` whitelists the field, the focused router/config/golden cluster passed (`89`), the broader smoke/determinism/cache/pipeline selectors passed (`17`), the config API E2E selector passed, and Opus post-diff audit found no hidden drift outside the intended seam
- the first exact exercising control surface for that seam is now fixed to `tBTCUSD`, `3h`, `2024-01-01 -> 2024-01-20`, warmup `120`, `curated_only`, and carrier `tmp/policy_router_evidence/tBTCUSD_3h_slice8_runtime_bridge_weak_pre_aged_release_guard_20260424.json`: baseline hits `11` continuation-release rows on `2024-01-17 15:00 -> 2024-01-19 06:00`, while `continuation_release_hysteresis = 0` collapses the late `2024-01-18` hysteresis block into earlier continuation support (`5` policy flips, `1` size change) but still leaves action count and top-line metrics unchanged on this exact subject
- the first deterministic subject-ranking map for that seam is now fixed to the monthly inventory grid `2016-07-01 -> 2026-03-31` (`117` full months, `curated_only`, warmup `120`, same weak-pre-aged release-guard carrier): `54` months are seam-active, none produce action-level divergence, and the strongest bounded monthly top-line-divergent subject is now `2021-08`, where `continuation_release_hysteresis = 0` improves final capital by `+18.890086341649294` with unchanged trade count and a six-row continuation-release cluster on `2021-08-18 -> 2021-08-19`
- the first dedicated exact top-line-divergent subject for that seam is now fixed to `tBTCUSD`, `3h`, `2021-08-01 -> 2021-08-31`, warmup `120`, `curated_only`, same weak-pre-aged release-guard carrier: baseline and release-zero preserve the same six continuation-release timestamps, trade count remains `7 -> 7`, `action_diff_count` remains `0`, but `continuation_release_hysteresis = 0` improves final capital by `+18.890086341649294` and return by `+0.18890086341649295` through bounded policy/size-path changes (`2` size diffs, `9` policy diffs, `9` reason diffs, `13` behavioral row diffs)
- the second dedicated positive exact top-line-divergent subject for that seam is now fixed to `tBTCUSD`, `3h`, `2025-10-01 -> 2025-10-31`, warmup `120`, `curated_only`, same weak-pre-aged release-guard carrier: baseline preserves a thirteen-row continuation-release cluster while `release_zero` exits that mode after the first ten rows, trade count remains `5 -> 5`, `action_diff_count` remains `0`, and `continuation_release_hysteresis = 0` improves final capital by `+17.134840124999755` and return by `+0.17134840124999756` through bounded policy/size-path changes (`2` size diffs, `6` policy diffs, `9` reason diffs, `19` behavioral row diffs)
- the first dedicated opposite-sign exact subject for that seam is now fixed to `tBTCUSD`, `3h`, `2018-03-01 -> 2018-03-31`, warmup `120`, `curated_only`, same weak-pre-aged release-guard carrier: baseline preserves a twelve-row continuation-release cluster while `release_zero` exits that mode after the first six rows, trade count remains `13 -> 13`, `action_diff_count` remains `0`, and `continuation_release_hysteresis = 0` underperforms by `-0.9371672999986913` USD and `-0.009371672999986924` return points despite the same bounded policy/size-path change shape (`2` size diffs, `6` policy diffs, `6` reason diffs, `35` behavioral row diffs)
- the complete full-calendar-month top-line-divergent bench for that seam is now closed as the three-subject exact triad `2021-08`, `2025-10`, `2018-03`: the frozen monthly windows artifact contains no fourth `topline_changed = true` month, the triad preserves unchanged trade count and `action_diff_count = 0` throughout, the same release-from-defensive policy/size-path mechanism is visible in every subject, and the resulting top-line sign is mixed rather than stable (`+`, `+`, `-`)

## Next admissible steps

Choose the smallest valid next step that matches the user request:

1. use `docs/decisions/regime_intelligence/policy_router/ri_policy_router_reanchor_post_aged_weak_closeouts_2026-04-27.md` and `docs/decisions/regime_intelligence/policy_router/ri_policy_router_aged_weak_active_carrier_truth_parked_handoff_2026-04-27.md` as the current truthful state anchors for this chain
2. do **not** reopen the findings-bank hardening proof unless a concrete CI discrepancy appears; the live-checkout and detached-worktree proof bundle is already green on this branch
3. the exact-carrier `defensive_probe` slice is now complete; on current repo-visible evidence it localizes to one tiny fresh low-zone pocket and does **not** justify runtime/config/default follow-up from this state alone
4. if the user wants to continue policy-improvement work on this chain, the full-calendar-month exact-subject bench is now closed on the current frozen grid, so any further continuation-release subject search requires a fresh packet that explicitly widens beyond that grid; without such a widening packet, keep December 2023 closed as null, January 2024 closed as exercising-but-topline-null control, and the exact top-line-divergent bench closed as the three-subject triad `2021-08`, `2025-10`, `2018-03`
5. if the user wants to continue the insufficient-evidence RI line after the completed July `2024`, late-2024, `2022-06`, exact `2019-06` vs `2022-06` D1 pair, exact `2019-06` vs `2025-03` second-pair survival slice, exact `2019-06` / `2022-06` / `2025-03` boundary-gap reread, exact `2019-06` vs `2020-10/11` source-backed third-control transport check, the completed four-surface synthesis re-anchor, and the completed four-surface context-clean falsifier, keep the bounded July-2024 -> `2022-06` translation chain parked and do **not** reopen it as transport logic. The current D1 line should now be treated as a bounded evidence bank with one bounded context-clean bank reread rather than as an automatically widening recurrence loop. Any further continuation must therefore start from a fresh packet posing a genuinely new falsifier or synthesis question, not from a fifth cheap recurrence check by default. Do **not** treat the current D1 bank read as runtime or concept authority, do **not** reopen March subjects, July as a primary subject, late-2024 as a recycled holdout, or the closed `2024` versus `2020` control logic without a new governed packet, and keep runtime, config-authority, and policy-promotion changes out of scope

## Hard stops

Stop and re-anchor before proceeding if any of the following happens:

- the next step starts relying on memory instead of cited anchors
- concept or research language starts implying runtime/family/authority status
- runtime/paper work starts to creep in without explicit authority
- a task touches strict-only surfaces or champion/promotion semantics
- the lane is no longer obvious from the latest user request

## Required day-start / resume ritual

At the start of a new day or resumed session, do this before reasoning forward:

1. read this file
2. read persistent user memory relevant to workflow
3. read current repo memory items relevant to the active lane
4. identify the latest validated lane and the latest non-active lanes
5. state the next smallest admissible step before making claims

## Update rule

Update this file only when one of these changes:

- active lane
- blocked/not-active lane status
- known verified blocker
- next admissible action

Keep it short. If detail is needed, point to the authoritative doc instead of copying it here.

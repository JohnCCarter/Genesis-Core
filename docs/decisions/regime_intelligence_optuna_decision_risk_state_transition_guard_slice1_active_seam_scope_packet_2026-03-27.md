# Regime Intelligence challenger family — DECISION risk-state transition-guard slice1 active-seam-scope packet

Date: 2026-03-27
Branch: `feature/ri-role-map-implementation-2026-03-24`
Status: `active-seam-scope / names-only / docs-only / no schema binding`

## COMMAND PACKET

- **Mode:** `RESEARCH` — source: `docs/governance_mode.md`
- **Category:** `docs`
- **Risk:** `MED` — why: this packet narrows the already approved names-only placement into a names-only definition of the active slice1 seam, but must remain narrower than values, narrower than schema, narrower than YAML authoring, and narrower than launch authority.
- **Required Path:** `Full gated docs-only`
- **Objective:** Record that the slice1 active seam is limited to the two already approved transition-guard leaf names under the same two already reserved subject paths, without approving values, ranges, types, ordering, nesting, serialization shape, YAML authoring, or launch authority.
- **Candidate:** `future tBTCUSD 3h RI DECISION transition-guard slice1 active seam scope`
- **Base SHA:** `d227be7e6d07c4b389529ee6a0ece228ca9a9b10`

### Scope

- **Scope IN:** one docs-only active-seam-scope packet; exact same two reserved subject paths; only the two already approved active slice1 leaf names; explicit statement that all other already placed leaf names remain upstream placement context but are outside slice1 change authority; deferred items; explicit red lines; explicit non-approval statement.
- **Scope OUT:** no source-code changes, no test changes, no changes under `src/core/**`, no changes under `tests/**`, no changes under `config/optimizer/**`, no changes under `tmp/**`, no changes under `results/**`, no YAML authoring, no file creation outside this one docs packet, no YAML/config/result/tmp file creation, no new leaf names, no renamed leaf names, no values, no defaults, no ranges, no grids, no step sizes, no units, no types, no requiredness, no ordering rules, no nesting approval, no serialization claims, no pseudo-YAML, no pseudo-JSON, no schema-like tree, no baseline-carry semantics, no inheritance semantics, no materialization claims, no launch, no validator/preflight/smoke execution, no gate claims, no runtime-valid RI conformity, no comparison/readiness/promotion/writeback.
- **Expected changed files:** `docs/decisions/regime_intelligence_optuna_decision_risk_state_transition_guard_slice1_active_seam_scope_packet_2026-03-27.md`
- **Max files touched:** `1`

### Gates required

For this packet itself:

- markdown/path sanity only
- manual wording audit that the packet remains names-only active-seam scope
- manual wording audit that the packet contains no value, type, range, ordering, nesting, serialization, or schema-binding semantics

For interpretation discipline inside this packet:

- only the two already approved transition-guard leaf names may be designated as the active seam
- only the same two reserved subject paths may be referenced
- all non-seam leaf names must be described only as upstream placement context, not as inherited, copied, fixed, or materialized content
- no sentence may imply that active-seam scope equals YAML approval, schema approval, or launch authority
- no sentence may imply how future files should be authored or rendered

### Stop Conditions

- any third leaf name inside the active seam
- any new path, fallback path, alias path, or auxiliary bucket
- any values, defaults, ranges, types, ordering rules, or nesting rules
- any pseudo-YAML / pseudo-JSON / schema sketch
- any statement that active-seam scope approves serialization shape or file authoring
- any wording that upgrades this packet into YAML preparation, launch preparation, or execution approval

### Output required

- one reviewable docs-only names-only active-seam-scope packet
- exact same two reserved subject paths
- exact two-leaf active seam declaration
- explicit out-of-slice1-change-authority statement for all other already placed leaf names
- exact deferred items / non-approval statement

## Purpose / decision ask

This packet answers one narrow question only:

- which already placed leaf names form the active slice1 seam for the `transition_guard` sizing surface under the same two already reserved subject paths?

This packet does **not** answer:

- what value either leaf should hold
- what range, grid, or step size either leaf should use
- what type either leaf should hold
- what order any field should appear in
- what nesting or serialization shape should be used
- whether any YAML should now be created
- whether any validator, preflight, smoke, execution, or launch step may begin

## Approved inputs only

This packet relies only on the already approved upstream chain, especially:

- `docs/decisions/regime_intelligence_optuna_decision_risk_state_transition_guard_slice1_leaf_field_inventory_packet_2026-03-27.md`
- `docs/decisions/regime_intelligence_optuna_decision_risk_state_transition_guard_slice1_leaf_placement_admissibility_packet_2026-03-27.md`
- `docs/decisions/regime_intelligence_optuna_decision_risk_state_transition_guard_slice1_names_only_leaf_placement_packet_2026-03-27.md`

No additional authority is opened here.

## Reserved subject paths

The only admissible subject paths remain:

- `config/optimizer/3h/ri_challenger_family_v1/tBTCUSD_3h_ri_decision_risk_state_transition_guard_slice1_2024_v1.yaml`
- `config/optimizer/3h/ri_challenger_family_v1/tBTCUSD_3h_ri_decision_risk_state_transition_guard_slice1_smoke_20260327.yaml`

No third path, fallback path, or alias path is opened here.

## Active seam — exact names only

The slice1 active seam is limited to exactly these two already approved leaf names:

- `parameters/multi_timeframe.regime_intelligence.risk_state.transition_guard.guard_bars`
- `parameters/multi_timeframe.regime_intelligence.risk_state.transition_guard.mult`

These two names are the only active slice1 seam leaves under this packet.

## Other already placed leaf names

All other already placed leaf names from the approved upstream placement packet remain part of the upstream placement record only.

Under this packet, those other names are:

- outside slice1 active-seam scope
- outside slice1 change authority
- not newly interpreted here as values, defaults, types, ordering rules, nesting rules, or file-authoring instructions

This packet does **not** state how any non-seam name is carried, rendered, serialized, inherited, copied, or materialized in any future file.

## Deferred items / unresolved questions

Still deferred beyond this packet:

- any values, defaults, ranges, grids, or step sizes for either seam leaf
- any types or units for either seam leaf
- any ordering or key sequence
- any nesting/tree layout or serialization shape
- any required/optional semantics
- any YAML authoring or file creation
- any validator/preflight/smoke evidence bundle
- any launch-admissibility or launch-authorization step
- any execution packet or outcome signoff

## Red lines / non-approval statement

This packet must not be read as:

- value approval
- range or grid approval
- type approval
- ordering approval
- nesting approval
- schema approval
- YAML authoring approval
- validator/preflight/smoke guidance
- launch guidance
- execution guidance

Any broader reading is out of scope.

## Bottom line

This packet does one narrow thing only:

- it limits the slice1 active seam to the two already approved `transition_guard` leaf names under the same two already reserved subject paths, using names only and without approving values, ranges, types, schema, YAML, or launch authority.

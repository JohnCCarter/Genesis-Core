# RI policy router insufficient-evidence D1 2017-06 corroborative packet

Date: 2026-05-07
Branch: `feature/next-slice-2026-05-06`
Mode: `RESEARCH`
Status: `completed / docs-only corroborative packet / packet-first bounded prep / non-authoritative / no behavior change`

Relevant skills claimed for this slice: `none`

This packet exists only to answer one bounded control-plane question from the workforce Agent B lane.
It is docs-only.
It does **not** reopen Agent A's `2023-06` falsifier lane.
It does **not** activate Agent C's `2023-04` fallback lane.
It does **not** authorize runtime/default/config/promotion work.

## COMMAND PACKET

- **Category:** `docs`
- **Mode:** `RESEARCH` — source: dispatch brief + base branch `feature/next-slice-2026-05-06`
- **Risk:** `LOW` — why: this slice writes one decision packet only, uses repo-visible markdown anchors only, and stays outside runtime/config/tests/scripts/results surfaces.
- **Required Path:** `Lite`
- **Lane:** `Research-evidence` — why this is the cheapest admissible lane now: the question is whether `2017-06` deserves a later corroborative opening, not whether a new implementation-ready discriminator or falsifier should run now.
- **Objective:** determine whether one exact `2017-06` low-zone suppression surface should remain available as a later corroborative external subject without duplicating Agent A's `2023-06` ownership tuple.
- **Candidate:** `2017-06 corroborative external subject framing`
- **Base SHA:** `cf852ad8a559dfd8313405c3c30806fd3ff00e08`
- **Skill Usage:** `none claimed` — docs-only reread over already-committed evidence anchors
- **Constraints:** `NO BEHAVIOR CHANGE`

## Why this packet exists

The allowed repo-visible anchors already establish three bounded facts that matter here:

1. the curated annual evidence marks `2017` and `2023` as the only mixed full years on the named annual surface,
2. the mixed-year shape comparison shows that `2017` and `2023` **share** June-led low-zone suppression,
3. the `2023` pocket isolation note shows that `2023` is not defined by June suppression alone because its stronger distinctiveness sits in December-led combined and continuation mass.

That combination opens one narrow corroborative possibility:

> if control / integration later wants one external subject that tests the *shared* June-led suppression shape without pretending that `2023-06` is unique, then one exact `2017-06` suppression subject is admissible as a secondary corroborative lane.

That is narrower than a second implementation lane.
It is also narrower than reopening annual mining.

## Exact question

Should one exact `2017-06` low-zone suppression surface be opened later as a corroborative external subject, without duplicating Agent A's `2023-06` falsifier lane or widening into a second implementation lane prematurely?

## Evidence anchors

The reasoning in this packet is bounded to the following repo-visible inputs only:

- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_2023_vs_2017_mixed_year_shape_comparison_2026-05-06.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_2023_mixed_year_pocket_isolation_2026-05-06.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_enabled_vs_absent_curated_annual_evidence_2026-04-28.md`
- `workforce_roadmap.md`
- `docs/governance/worker_governance_envelope.md`
- `docs/decisions/governance/workforce/workforce_v1_wave1_cloud_batch_dispatch_2026-05-07.md`
- `docs/decisions/governance/workforce/workforce_v1_wave1_agent_b_d1_2017_06_cloud_dispatch_2026-05-07.md`

## Observed anchors

From the allowed inputs only, the packet can state all of the following directly:

- `2017` and `2023` are the only mixed full years named in the curated annual evidence note.
- On the fixed mixed-year comparison, the top suppression month is `June` in both `2017` and `2023`.
- On that same comparison, the top combined and top continuation months diverge across the two years.
- On the `2023` pocket-isolation note, `2023-06` is the top suppression month, but `2023` is not reduced to a pure June-only annual story.
- The workforce dispatch matrix assigns Agent A ownership of `2023-06` / `D1 external falsifier` / `implementation-prepared deep-dive`, while Agent B owns `2017-06` / `corroborative packet framing` / `packet-first bounded prep`.

## Inferred bounded read

The smallest honest inference is:

> yes — one later exact `2017-06` suppression subject may be kept open as a **corroborative** external surface, precisely because June-led suppression is shared across the two mixed years while `2023`-specific distinctiveness appears elsewhere.

But that inference is bounded in four ways:

1. it supports only a **later** corroborative opening,
2. it does **not** support a second implementation-ready lane,
3. it does **not** let Agent B inherit Agent A's selector bank or output class,
4. it does **not** prove that `2017-06` will yield a useful discriminator once exact rows are locked.

## Non-overlap guardrails

This packet must remain explicitly non-overlapping with the other batch lanes.

### Agent A non-overlap

This packet does **not** duplicate Agent A's `2023-06` lane.
It does **not** reuse Agent A's target month, selector framing, falsifier semantics, or implementation-prepared output class.
It does **not** claim that the next step is immediate execution on `2023-06` from the Agent B lane.

### Agent C non-overlap

This packet is **not** Agent C's `2023-04` fallback lane.
It does **not** define fallback activation conditions.
It does **not** reactivate dormant fallback logic by analogy.

### Subject-width guardrail

This packet stays bounded to `2017-06` only.
It does **not** widen into March, July `2024`, late-2024, `2023-06`, `2023-04`, or annual-wide rescans.

## Later exact subject to keep available

If control / integration later chooses to open this corroborative line, the exact subject should remain this and nothing broader:

> **one exact `2017-06` low-zone `insufficient_evidence` suppression external subject on the fixed enabled-vs-absent annual family surface, with absent action `LONG`, enabled action `NONE`, and `2017-06` as the full month boundary.**

Additional guardrails for that later slice:

- any `AGED_WEAK_CONTINUATION_GUARD` row may be described as sibling context only, not folded into the primary target by default,
- the later slice must lock one exact row set inside `2017-06` rather than reopening all of `2017`,
- the later slice must fail closed if the subject only becomes meaningful by adding March, annual rescans, or a second year pair.

## Optional bounded planning appendix

If this corroborative lane is reopened later, the smallest honest plan is:

1. use only repo-visible evidence already admissible on the chosen branch,
2. lock one exact `2017-06` row set inside the fixed low-zone `LONG -> NONE` suppression family,
3. describe the subject as an external corroborative control only,
4. compare nothing outside the exact `2017-06` subject unless a fresh packet re-authorizes that widening,
5. return a single bounded note stating whether the locked `2017-06` subject corroborates the shared June-led suppression read descriptively.

Not allowed in that later plan:

- helper/test/artifact creation from this packet alone,
- selector fitting by inheritance from Agent A,
- transport, runtime, readiness, default, or promotion claims,
- any claim that corroboration alone makes `2017-06` a new primary lane.

## Scope

- **Scope IN:**
  - `docs/decisions/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_2017_06_corroborative_packet_2026-05-07.md`
  - optional bounded planning appendix inside the same packet file
- **Scope OUT:**
  - all `2023-06` files owned by Agent A
  - all `2023-04` files owned by Agent C
  - `scripts/**`
  - `tests/**`
  - `results/evaluation/**`
  - `docs/analysis/**`
  - `src/**`
  - `config/**`
  - `GENESIS_WORKING_CONTRACT.md`
  - March, July `2024`, late-2024, and annual-wide rescans

## Stop conditions

Stop and fail closed if any of the following becomes necessary:

- duplicating Agent A's `2023-06` implementation-ready output class,
- widening beyond `2017-06` to make the corroborative subject seem meaningful,
- creating code, tests, helpers, or result artifacts to justify the packet immediately,
- implying runtime/default/readiness/promotion authority,
- pretending that the later exact row lock is already proven by the current markdown-only evidence.

## Required return shape for any later execution slice

A later execution slice, if ever opened, should return only:

- `status`
- `packet_path`
- `summary`
- `observed`
- `inferred`
- `unverified`
- `what_this_does_not_prove`
- `recommended_next_step`
- `recommended_integration_class`
- `base_sha_confirmed`
- `scope_adherence_report`

## What this packet does not prove

This packet does **not** prove:

- that a useful `2017-06` exact row lock already exists,
- that `2017-06` reproduces Agent A's future `2023-06` outcome,
- that June-led suppression carries runtime or D1 discriminator authority on its own,
- that `2017-06` should become a primary implementation lane,
- that mixed-year annual overlap alone is enough to justify code, tests, or promotion work.

## What this packet does not authorize

This packet does **not** authorize:

- reopening `2023-06` from Agent B,
- activating Agent C,
- any new helper, test, JSON artifact, or analysis note,
- any runtime/default/config/policy/promotion change,
- any shared-truth or roadmap/backlog promotion write.

## Recommended next step

Control / integration should keep **one later exact `2017-06` corroborative subject** available as a parked secondary follow-up, and only activate it if Agent A's primary `2023-06` lane later benefits from one bounded shared-shape corroboration surface.

## Recommended integration class

`park`

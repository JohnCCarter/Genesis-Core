# Edge Map

> This document is derivative and citation-bound. It routes to existing canonical artifacts and does not create new authority, readiness, or research conclusions.

## Scope boundary

This file exists in the current slice only as a fail-closed edge-classification stub.
It does **not** yet record active cross-document edge claims.

Population of this map requires:

- direct classified support for the edge being claimed
- exact citation to the surface on both sides of the edge
- a bounded statement of what the edge means and what it does not mean

If those conditions are not met, the correct state is `UNRESOLVED`.

## Current status

- File status in this slice: `UNRESOLVED`
- Active edge entries recorded in this slice: `NONE`

## Why no active entries are recorded here yet

This first pass created a routing/index layer for top-level governance and documentation surfaces only.
It did not yet classify domain-specific evidence-to-authority edges, supersession edges, or lineage-bearing packet relationships deeply enough to record them here safely.

See:

- `docs/knowledge/KNOWLEDGE_AUTHORITY_RULES.md` (`Stub rule for lineage and edge maps`)
- `docs/CURRENT_AUTHORITY_INDEX.md` (`Explicitly unresolved in this slice`)
- `docs/knowledge/CANONICAL_KNOWLEDGE_REGISTER.md` (`Scope boundary for this slice`)

## Future admission rule

An edge entry may be added only in a later bounded slice that:

- cites both endpoint artifacts directly
- states whether the edge is observed, inferred, or still unverified
- does not infer supersession or authority from chronology, folder, or prominence alone
- keeps unresolved boundaries marked `UNRESOLVED`

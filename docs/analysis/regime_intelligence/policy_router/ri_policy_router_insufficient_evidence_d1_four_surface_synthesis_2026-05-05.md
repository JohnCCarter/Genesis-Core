# RI policy router insufficient-evidence D1 four-surface synthesis

Date: 2026-05-05
Branch: `feature/next-slice-2026-04-29`
Mode: `RESEARCH`
Status: `completed / docs-only synthesis / bounded four-surface re-anchor`

This note closes and re-anchors the **current** insufficient-evidence D1 chain after four bounded exact-surface anchors are now in place.
It does **not** reopen the parked July `2024` translation chain.
It does **not** create a fifth recurrence surface.
It does **not** authorize runtime/default/policy/config/promotion work.

Its purpose is narrower:

> state the smallest honest reading of what the current D1 non-age family now supports across the fixed `2019-06`, `2022-06`, `2025-03`, and `2020-10/11` chain, and state clearly what that chain still does **not** justify.

## COMMAND PACKET

- **Category:** `obs`
- **Mode:** `RESEARCH` — source: live branch `feature/next-slice-2026-04-29`
- **Risk:** `LOW`
- **Required Path:** `smallest admissible RESEARCH docs-only path`
- **Lane:** `Research-evidence`
- **Objective:** synthesize the completed four-surface D1 chain into one bounded, non-authoritative closeout before any further widening or candidate discussion.
- **Candidate:** `docs-only D1 four-surface synthesis / re-anchor`
- **Base SHA:** `6ac59ef0c08cb3328348d5e64ad40d83ccd4f9f9`
- **Skill Usage:** no existing repo skill directly fits this docs-only synthesis slice; no additional skill coverage is claimed beyond the already-completed helper-backed evidence slices being summarized
- **Opus pre-code verdict:** `APPROVED_WITH_NOTES`

## Evidence inputs

- `docs/decisions/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_four_surface_synthesis_precode_packet_2026-05-05.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_translation_parking_synthesis_2026-05-04.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_exact_subject_pair_2019_06_vs_2022_06_2026-05-04.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_family_survival_2019_06_vs_2025_03_2026-05-04.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_boundary_gap_to_local_context_floor_2026-05-04.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_boundary_gap_transport_check_2019_06_vs_2020_10_11_2026-05-04.md`
- `GENESIS_WORKING_CONTRACT.md`

## Four-surface recap

### 1. The first exact pair established a real non-age D1 answer, but only pair-locally

The completed exact D1 pair `2019-06 harmful` vs `2022-06 weak control` established that the line had moved beyond a pure age-only story.
On that fixed pair, three direct decision-time non-age fields each separated the exact target rows cleanly:

- `action_edge`
- `confidence_gate`
- `clarity_raw`

That was already a meaningful bounded result.
But it was also clearly context-leaky:

- the harmful sibling row was still selected,
- and the exact `2022-06` continuation-context quartet was still selected.

So the first pair answered:

> a real non-age D1 family exists on this exact harmful/control pair,

not:

> a clean broader local-context discriminator already exists.

### 2. The second exact pair showed that the same non-age family survives unchanged

The completed `2019-06` vs `2025-03` survival slice asked the next honest recurrence question without changing the family.
It showed that the same three non-age fields survived unchanged on the second exact pair.

That mattered in two ways:

- the family was no longer just a `2019-06` vs `2022-06` one-off,
- and the descriptive age-only comparator no longer carried the answer on the second pair.

But the same limitation remained:

- the surviving family still selected the full fixed context envelope.

So after the second pair, the line was stronger, but still not classifier-clean.

### 3. The tri-surface boundary-gap reread changed the best description of the family

The completed exact `2019-06` / `2022-06` / `2025-03` boundary-gap reread answered the next residual question:

> is the recurring family best read merely as a repeated absolute threshold, or as something more structurally honest on the fixed surfaces?

On the exact tri-surface, the harmful blocked target sat materially closer to its local context floor than both weak-control targets did on all three admitted non-age fields:

- `action_edge`
- `confidence_gate`
- `clarity_raw`

That sharpened the interpretation.
The family was now better read as an **envelope-relative boundary-gap structure** on the fixed surfaces, not merely a threshold that happened to win twice.

That was stronger than pair survival alone.
It was still exact-surface only.

### 4. The third-control transport check closed the remaining 2020 field-admission uncertainty

The exact `2019-06` vs exact `2020-10/11` transport check was the narrowest remaining cheap recurrence question.
Before it ran, the honest concern was whether the `2020` side might fail closed because the older truth-surface artifact had not materialized `clarity_raw`.

That concern is now closed.
The completed source-backed reread showed that the raw locked `2020` action-diff rows do carry direct `clarity_raw` on all required target/context rows.

And once that admission held, the same boundary-gap ordering survived again on:

- `action_edge`
- `confidence_gate`
- `clarity_raw`

So the current D1 line is no longer “two pairs plus one reread.”
It now includes one additional source-backed third-control recurrence check with direct `clarity_raw` support.

## Honest synthesis

The smallest honest reading of the current chain is now:

> the fixed `2019-06` harmful anchor and the fixed weak-control set `2022-06`, `2025-03`, and source-backed `2020-10/11` jointly support a bounded recurring non-age D1 family on `action_edge`, `confidence_gate`, and `clarity_raw`, and the best current read of that family is exact-surface, envelope-relative recurrence rather than one-off pair luck.

That is the strongest honest sentence.
It is stronger than the earlier pair-local read.
It is also still narrower than any runtime or portable transport claim.

## What the chain now supports

The current four-surface D1 chain supports all of the following bounded statements:

1. the fixed harmful anchor remains `2019-06`
2. the weak-control side now includes three exact weak-control surfaces:
   - exact `2022-06`
   - exact `2025-03`
   - exact source-backed `2020-10/11`
3. the same non-age family recurs across the completed exact-pair / second-pair / boundary-gap / third-control chain:
   - `action_edge`
   - `confidence_gate`
   - `clarity_raw`
4. the `2020-10/11` control is no longer under a direct field-admission cloud because `clarity_raw` was checked on the raw locked rows rather than inferred from the older truth-surface note
5. the best current structural read is envelope-relative boundary-gap recurrence on the fixed surfaces, not just one clean absolute-threshold coincidence
6. the current line is materially stronger than it was before the `2020` transport check

## What the chain still does **not** support

The current four-surface D1 chain still does **not** support any of the following:

- a portable runtime boundary-gap rule
- a globally stable discriminator claim
- a clean local-context classifier that excludes the full fixed context envelope
- a statement that context leakage is solved
- a statement that the parked July `2024` translation chain has been reopened or redeemed
- a statement that recurrence alone now justifies more widening by default
- runtime/default/policy/config/family/champion/promotion/readiness authority

This is the part most likely to drift if it is not written down clearly.
The current chain is stronger than before.
It is not authorizing.

## Why the limitation still matters

The four-surface chain is now recurrence-stronger, but it has not changed category.
The same family that survives on the exact target/control surfaces is still not established here as a clean separator against the full nearby local envelope.

So the honest category remains:

- bounded research evidence
- exact-surface only
- observational only
- non-authoritative

That category is not a hedge.
It is the correct scope fence for the chain as it currently exists.

## Re-anchor verdict

The current D1 line should now be treated as a **re-anchored bounded evidence bank**, not as an automatically widening micro-slice loop.

That means:

- do not keep adding cheap recurrence checks by default,
- do not silently promote the current chain into candidate or runtime authority,
- do not reopen the parked July line as if this D1 chain had repaired it,
- do not treat four-surface recurrence as equivalent to classifier-clean or transport-clean proof.

## Reopen rule

If this line is ever continued again, it must start from a **fresh packet** posing a genuinely new question.
That reopen may not begin with:

- a fifth recurrence helper by default,
- silent widening to a new year surface,
- or a restatement of the current four-surface recurrence as if it had already become authority.

A valid reopen must instead ask something genuinely new, for example:

- a bounded falsifier on context-clean selectivity,
- a bounded synthesis question at a different interpretation layer,
- or a policy-facing question that explicitly keeps runtime authority out of scope.

Those are examples of shape only.
They are not authorized next actions by this note.

## Current end state of this lane

The current insufficient-evidence D1 line is now honestly re-anchored as follows:

- exact `2019-06` harmful anchor fixed
- exact `2022-06` weak-control recurrence fixed
- exact `2025-03` weak-control recurrence fixed
- exact source-backed `2020-10/11` weak-control recurrence fixed
- same non-age family recurring on `action_edge`, `confidence_gate`, and `clarity_raw`
- best read = exact-surface envelope-relative recurrence
- remaining category = bounded research evidence only

That is the present honest end state of **this** four-surface D1 chain.

{
  "governance": {
    "skill_type": "RUNNER",
    "contract_owner": "opus-4.6",
    "implementation_owner": "codex-5.3",
    "pass_definition_locked": true,
    "scope_locked": true,
    "evolution_mode": "additive_only",
    "forbidden": [
      "runtime_default_drift",
      "api_contract_drift",
      "scope_broadening",
      "weakening_must_not_rules"
    ],
    "stop_conditions": [
      "detected_default_drift",
      "detected_contract_drift",
      "attempted_scope_change"
    ]
  },
  "id": "genesis_backtest_verify",
  "version": "1.0.0",
  "title": "Backtest verification (deterministic v1)",
  "description": "Vertical slice v1: validates registry + compares backtest artifacts deterministically. Intended to be runnable via scripts/run_skill.py.",
  "status": "stable",
  "locked": true,
  "owners": ["fa06662"],
  "tags": ["backtest", "verification", "determinism"],
  "steps": [
    { "id": "validate_registry", "type": "registry_validate", "args": {} },
    {
      "id": "compare",
      "type": "compare_backtest_results",
      "args": {
        "baseline_path": "registry/fixtures/backtest_sample.json",
        "candidate_path": "registry/fixtures/backtest_sample.json"
      }
    }
  ],
  "rules": {
    "must": [
      "Kör deterministiskt (ingen slump).",
      "Inga hemligheter i loggar eller artifacts."
    ],
    "must_not": [
      "Skapa eller modifiera trading artifacts utan explicit flagga."
    ]
  },
  "references": [
    {
      "type": "doc",
      "value": "docs/templates/skills/genesis_backtest_verify.md"
    }
  ]
}

# Legacy + RI research Phase 3 Legacy class opening

Date: 2026-03-27
Status: tracked / analysis-only / class-opened / no implementation authority
Lane: `run_intent: research_experiment`
Phase: `3 - Legacy internal research track`
Selected class: `B. Legacy internal next-hypothesis experiments`

## Purpose

This artifact opens the Legacy internal research class after the governed return from the exhausted RI-local chain.

This artifact does not authorize implementation, config changes, launches, comparison, readiness, promotion, runtime changes, or production writeback.

## Current state entering this class opening

- The RI regime-calibration slice was classified `degradation`.
- The RI balanced-conflict decision slice was classified `degradation`.
- The RI objective probe was classified `plateau`.
- The current RI parameterization chain is therefore treated as locally exhausted for the presently opened line of inquiry.
- The governed roadmap order still favors a Legacy-internal check before any cross-family coordination, router, or ensemble lane is opened.

## Why class B is opened now

- It is orthogonal to the most recent RI slices because it moves the search to the Legacy family rather than another RI-local surface.
- It preserves family separation by evaluating Legacy on its own internal terms rather than importing RI semantics.
- It tests whether Legacy still has productive internal research room before widening scope into coordination or router work.
- It keeps the lane fail-closed by opening only the class, not a concrete Legacy slice.

## Carry-forward constraints

- Legacy and RI remain separate strategy families.
- Mixed family surfaces must not be reintroduced.
- Legacy internals must not import RI thresholds, RI gates, RI calibration logic, or RI family semantics.
- Research outputs from this lane remain research-only unless a later lane is explicitly opened.
- This artifact opens class `B` only; it does not open a concrete Legacy slice.

## Allowed future Legacy subtracks

- `L-S1` - Legacy signal hypothesis
- `L-S2` - Legacy regime or context hypothesis
- `L-S3` - Legacy decision or gating hypothesis
- `L-S4` - Legacy objective or robustness hypothesis

## Explicit non-goals

- no concrete `L-S1` / `L-S2` / `L-S3` / `L-S4` slice opening in this artifact
- no Legacy implementation work
- no config creation or launch authorization
- no new RI slice from the exhausted current chain
- no cross-family coordination, router, or ensemble opening
- no comparison, readiness, or promotion opening
- no runtime default or champion mutation

## Class-opening decision rule

- The next artifact in this lane must choose exactly one Legacy subtrack.
- That next artifact must define one bounded Legacy hypothesis only.
- That next artifact must explicitly state allowed surface, fixed surface, and non-goals for the selected Legacy subtrack.
- If the next step cannot keep Legacy research isolated from RI semantics, fail closed and return to governed selection.

## Next admissible step

- The next admissible step after this class opening is one bounded Legacy packet for exactly one of the allowed Legacy subtracks.
- That next step must remain docs-first, single-subtrack, and research-only.
- That next step must not combine implementation and execution in the same move.

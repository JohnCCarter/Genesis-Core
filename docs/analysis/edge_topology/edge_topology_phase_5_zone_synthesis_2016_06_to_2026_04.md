# Genesis-Core Edge Topology — Phase 5 zone synthesis

Date: 2026-05-26
Branch: `feature/research-attribution-layer-foundation-2026-05-26`
Mode: `RESEARCH`
Base SHA anchor: `fe591061`
Status: `completed / provisional zone registry / observational and non-authorizing`

## Purpose

This slice promotes only the repeated and evidenced structures from Phases 2–4 into provisional topology zones.

The labels used here are controlled and intentionally modest.
They describe the current research surface only.
They do **not** create runtime authority.

## Scope

### Scope IN

- provisional zone labels drawn only from current retained evidence
- explicit registration of rejected, superseded, weak, active, and unknown structures
- separation between structural zones, boundary claims, and evidence gaps

### Scope OUT

- runtime recommendations
- any label that silently implies promotion-ready truth
- any zone promotion from one-off unreplicated local behavior alone

## Evidence inputs

- `results/research/edge_topology/phase_4_failure_mode_maps/phase_4_failure_and_anti_edge_map_2016_06_to_2026_04.json`
- `results/research/edge_topology/phase_3_attribution_maps/phase_3_attribution_map_annual_primary_with_monthly_sidecar_2019_2025.json`

## Emitted artifact

- `results/research/edge_topology/phase_5_zone_synthesis/phase_5_provisional_zone_registry_2016_06_to_2026_04.json`

## Label meaning on this lane

- `ACTIVE` = strongest repeated boundary currently supported on this research surface
- `WEAK` = repeated but still sign-unstable, small-bench, or otherwise limited
- `REJECTED` = contradicted by current retained evidence
- `SUPERSEDED` = earlier framing replaced by a tighter one on the same evidence chain
- `UNKNOWN` = still under-supported on current retained surfaces

## Observed zone registry

### 1. One zone is currently strong enough to mark `ACTIVE`

#### `blocked_baseline_long_quality_inversion_boundary` -> `ACTIVE`

This is the clearest repeated boundary in the lane.
It is supported across grouped positive and negative years, not just one local period.

Smallest honest reading:

- when the router is still suppressing blocked baseline longs that no longer look clearly weak on the same proxy surface, the harmful side of the annual topology becomes more likely.

### 2. Four structures are currently promoted only as `WEAK`

#### `annual_shared_late_low_zone_pocket` -> `WEAK`

Repeated everywhere, but not sign-discriminative.
It is important background topology, not strong edge by itself.

#### `continuation_dominant_control_positive_relative_cluster` -> `WEAK`

Useful relative-beneficial annual cluster, but still bounded and not universal.

#### `suppressor_heavier_control_negative_relative_cluster` -> `WEAK`

Useful relative-harmful annual cluster, but still bounded and not universal.

#### `monthly_early_band_release_sign_pattern` -> `WEAK`

The frozen exact-subject triad supports a local sign pattern, but the month bench remains too small for stronger promotion.

### 3. Two earlier stories are now explicitly `REJECTED`

#### `absolute_top_equals_control_edge_claim` -> `REJECTED`

`2024` alone falsifies this shortcut.
Absolute top-line strength does not automatically mean control-relative edge.

#### `negative_year_unique_shared_pocket_claim` -> `REJECTED`

Positive and negative years both carry the same broad late low-zone shared pocket.
So uniqueness was the wrong boundary.

### 4. One older explanation is now `SUPERSEDED`

#### `single_aged_weak_root_cause_claim` -> `SUPERSEDED`

The sharper current read is a mixed blocked cohort with displacement plus suppression, and a stronger local suspect on the `insufficient_evidence` seam.

### 5. Two important areas remain `UNKNOWN`

#### `monthly_general_control_surface` -> `UNKNOWN`

The current month surface still lacks a general `enabled vs absent` control map.

#### `flat_period_structural_coherence` -> `UNKNOWN`

Flat periods remain useful anchors, but they are not yet proven to form one coherent topology class.

## Inferred

### 1. The lane is now structured enough to summarize honestly

After Phase 5, the lane no longer consists only of raw findings.
It now has:

- one strongest active anti-edge boundary
- a small set of weak but repeated structural clusters
- clear rejected shortcuts
- explicit unknowns

That is enough to close the lane with a canonical summary.

### 2. The registry is intentionally asymmetric

There is currently a stronger `ACTIVE` anti-edge boundary than `ACTIVE` edge zone.
That is not a flaw.
It simply reflects what the retained evidence currently supports.

## Unverified

- This slice does **not** prove that the `WEAK` zones will survive a widened period bench.
- This slice does **not** prove that the `ACTIVE` annual anti-edge boundary is the only one that matters.
- This slice does **not** convert any zone label into runtime or promotion authority.

## Post-validation addendum (2026-05-27)

After the bounded validation slice in `docs/analysis/edge_topology/edge_topology_active_boundary_validation_2026-05-27.md`, the registry reading tightens in one specific place.

### Observed

- `blocked_baseline_long_quality_inversion_boundary` remains `ACTIVE`
- that label now reads only at the **grouped annual proxy** level
- the label should **not** currently be read as:
	- a clean single-carrier rule
	- a portable exact-surface transport rule
	- a runtime target
	- a global threshold law

### Carrier parking state

The validation slice also parks the inner carrier stories below the label itself:

- mixed blocked action-layer explanation -> `WEAK`
- exact-surface `insufficient_evidence` D1 carrier -> `WEAK`
- broader local harmful `2024` shell -> `WEAK`

### Consequence for the registry

The registry does **not** lose its only `ACTIVE` zone.
But the `ACTIVE` zone is now narrower and cleaner than the original Phase 5 wording alone.

## Consequence

Phase 5 is now complete.
The lane has a provisional zone registry and can close with one canonical summary.

## What changed and what did not

What changed:

- the lane now has a controlled label registry rather than an implicit mental map
- rejected shortcuts and unknown gaps are now explicit first-class outputs
- the strongest repeated anti-edge boundary is now formally parked as `ACTIVE` on the current research surface

What did **not** change:

- no runtime claims were made
- no zone was promoted from one-off anecdotal evidence alone
- no `WEAK` or `ACTIVE` label implies production authority

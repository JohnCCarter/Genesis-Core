# Genesis-Core Edge Topology — ACTIVE boundary carrier adjudication

Date: 2026-05-27
Branch: `feature/research-attribution-layer-foundation-2026-05-26`
Mode: `RESEARCH`
Base SHA anchor: `be5e03a0`
Status: `completed / bounded carrier adjudication / observational only`

## Purpose

This slice asks the next smallest question after the completed active-boundary validation:

> does any carrier beneath the sole `ACTIVE` Edge Topology boundary currently deserve promotion above `WEAK`?

The target boundary remains:

- `blocked_baseline_long_quality_inversion_boundary`

This slice does **not** reopen broad annual generation, runtime interpretation, or another helper-backed loop by default.
It only adjudicates the current carrier layer using already-retained evidence.

## Scope

### Scope IN

- adjudicate the current carrier layer beneath the sole `ACTIVE` anti-edge boundary
- compare grouped annual support against carrier-specific narrowing and falsification surfaces
- decide whether any carrier should be promoted above `WEAK`
- emit one docs note and one machine-readable carrier verdict

### Scope OUT

- new helper-backed evidence generation
- new runtime/default/policy claims
- reopening monthly control unification
- editing the currently locally modified canonical Phase 5 / Phase 6 docs in this slice

## Evidence inputs

- `docs/analysis/edge_topology/edge_topology_active_boundary_validation_2026-05-27.md`
- `results/research/edge_topology/active_boundary_validation/active_boundary_validation_verdict_bundle_2026-05-27.json`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_blocked_reason_split_2026-04-29.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_four_surface_synthesis_2026-05-05.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_d1_late_2024_external_surface_falsifier_2026-05-06.md`
- `docs/analysis/regime_intelligence/policy_router/ri_policy_router_fixed_three_cohort_phase_discriminator_2026-05-26.md`

## Emitted artifact

- `results/research/edge_topology/active_boundary_validation/active_boundary_carrier_adjudication_2026-05-27.json`

## Observed

### 1. The mixed blocked action-layer explanation still has the widest annual relevance, but it is too mixed to promote

The blocked baseline-long annual divider does remain the strongest grouped annual proxy boundary.
But the blocked action-layer explanation remains compositionally mixed:

- largest blocked bucket: `stable_continuation_state`
- large true-suppressor buckets: `AGED_WEAK_CONTINUATION_GUARD`, `insufficient_evidence`
- displacement and suppression still share the same aggregate blocked action-layer surface

So this carrier layer is broad and important, but still not pure enough to promote beyond `WEAK`.

### 2. The exact-surface `insufficient_evidence` D1 family is the strongest narrow carrier, but it remains bounded

The fixed D1 chain still supports one real narrow carrier family on:

- `action_edge`
- `confidence_gate`
- `clarity_raw`

across the fixed harmful `2019-06` anchor and the weak-control set:

- `2022-06`
- `2025-03`
- `2020-10/11`

That is meaningful.
But it still remains:

- exact-surface only
- context-leaky
- non-authoritative

So it is the strongest narrow carrier candidate without yet becoming a promotable one.

### 3. The first fresh external falsifier blocks promotion of the exact-surface D1 carrier

The late-2024 external-surface falsifier matters directly for adjudication.
It showed that unchanged transport of the D1 bank fails on the first fresh external surface.

That means the exact-surface `insufficient_evidence` carrier cannot currently be promoted above `WEAK`, because:

- recurrence exists,
- but external portability does not yet hold.

### 4. The broader local harmful `2024` shell remains real, but still local

The fixed three-cohort phase discriminator gives a second non-trivial carrier candidate.
It shows that the harmful `2024` target pocket sits on a weaker local shell than the two fixed `2023-12` continuation waves.

That supports a broader local-shell reading.
But it still remains:

- fixed-subject only
- not externally transported
- not a portable annual rule

So it also remains `WEAK`.

### 5. No current carrier falsifies the grouped annual boundary, but no carrier clears promotion either

This is the key adjudication balance.

The carrier layer does enough to support interpretation.
It does **not** do enough to replace the grouped annual proxy boundary with a stronger promoted inner rule.

## Inferred

### 1. The current best map is intentionally top-heavy

The map is strongest at:

- grouped annual proxy boundary level

and weaker below that at:

- carrier level

That asymmetry is now evidence-backed rather than accidental.

### 2. The exact right current outcome is “keep all carriers `WEAK`”

The mixed blocked action-layer explanation is too impure.
The exact-surface `insufficient_evidence` D1 family is too bounded and transport-limited.
The broader local harmful `2024` shell is too local.

So the smallest honest adjudication result is:

- **no carrier promotion at this time**

### 3. Any stronger carrier claim now would require genuinely new evidence, not rephrasing

A carrier could later be revisited.
But on the current retained surfaces, stronger wording would require a new decisive question rather than another summary pass.

## Unverified

- whether a second fresh external falsifier would further weaken the narrow `insufficient_evidence` carrier
- whether a subject-relative or normalized phase-age transport test would strengthen or weaken the broader local harmful-shell reading
- whether any later carrier can rise above `WEAK` without widening the retained evidence surface

## Verdict

### Carrier states after adjudication

- `mixed_blocked_action_layer_explanation` -> **keep `WEAK`**
- `insufficient_evidence_exact_surface_d1_family` -> **keep `WEAK`**
- `broader_local_harmful_2024_shell` -> **keep `WEAK`**

### Carrier promotion result

- **no carrier is promoted above `WEAK`**

### What remains strongest

The strongest current read therefore remains:

- `blocked_baseline_long_quality_inversion_boundary` stays `ACTIVE`
- that `ACTIVE` label remains valid only at the grouped annual proxy level
- the full carrier layer remains interpretive support, not stronger authority than the boundary itself

## Consequence

This slice closes the immediate follow-up question after the active-boundary validation.

The repo now has an explicit answer to:

> should any carrier beneath the sole `ACTIVE` anti-edge boundary currently be promoted?

Current answer:

- **no**

That is useful because it prevents silent drift from:

- grouped boundary truth

into:

- over-claimed carrier truth.

## What changed and what did not

What changed:

- the current carrier layer has now been explicitly adjudicated instead of being left as an implied interpretation
- the repo now states clearly that no carrier currently deserves promotion above `WEAK`
- the asymmetry between one `ACTIVE` grouped boundary and weaker carrier explanations is now frozen intentionally

What did **not** change:

- no runtime behavior changed
- no new helper-backed evidence was generated
- no carrier was promoted into authority
- the locally modified canonical Phase 5 / Phase 6 docs were left untouched by this slice

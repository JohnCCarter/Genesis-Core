# Genesis-Core Edge Topology — ACTIVE boundary validation kickoff

Date: 2026-05-27
Branch: `feature/research-attribution-layer-foundation-2026-05-26`
Mode: `RESEARCH`
Base SHA anchor: `921ed06a`
Status: `started / docs-first bounded validation kickoff / observational only`

## Purpose

This slice starts the next bounded Edge Topology follow-up after the completed Phase 6 map.

Its only job is to validate the current `ACTIVE` label on:

- `blocked_baseline_long_quality_inversion_boundary`

It does **not** open runtime work.
It does **not** widen into a new annual screening pass.
It does **not** silently recycle older D1 micro-slices as if they had already upgraded the boundary into authority.

## Mode proof

This work remains `RESEARCH` because the active branch is `feature/*` and the intended touched surfaces stay inside docs/results research artifacts.

What `RESEARCH` allows here:

- smallest admissible bounded research step
- docs/results-only synthesis and validation planning
- explicit `observed`, `inferred`, and `unverified` separation
- later read-only helper work if a genuinely new bounded question is still needed

What remains forbidden here:

- runtime/default/config/policy changes
- promotion/readiness/champion claims
- silent widening into comparison-authority or strict-only surfaces

What would force `STRICT` escalation:

- touching champion/freeze paths
- entering runtime-default authority, readiness, or promotion surfaces
- modifying family-rule or other strict-only runtime surfaces

## Scope

### Scope IN

- validation of the existing `ACTIVE` zone label using already-retained evidence chains
- reconciliation of annual grouped support, blocked-cohort decomposition, exact-surface D1 evidence, and external falsifiers
- explicit statement of whether the current `ACTIVE` label should remain `ACTIVE`, be narrowed, or be downgraded

### Scope OUT

- new runtime helpers or router changes
- re-running broad annual evidence generation
- reopening month-surface unification
- claiming that one narrow carrier (`insufficient_evidence`, `AGED_WEAK_CONTINUATION_GUARD`, or raw age) is already the whole explanation

## Current validation question

The exact follow-up question is:

> does `blocked_baseline_long_quality_inversion_boundary` still deserve the `ACTIVE` label once we integrate both its strongest grouped-year support and the existing narrower challenge surfaces that limit transport, carrier purity, and local portability?

## Emitted kickoff artifact

- `results/research/edge_topology/active_boundary_validation/active_boundary_validation_source_inventory_2026-05-27.json`

## Observed

### 1. The current `ACTIVE` label was earned on a real grouped annual proxy split

The strongest retained support still comes from the grouped blocked baseline-long proxy comparison:

- negative full years: blocked cohort mean `+0.356117%`, median `+0.076866%`
- positive full years: blocked cohort mean `-0.172585%`, median `-0.261849%`

That is not trivial noise.
It is the cleanest current sign-separating annual proxy surface in the completed topology lane.

### 2. The grouped boundary is already narrower than a simple “bad pocket exists” story

The boundary does **not** sit on shared-pocket presence alone.
That broader explanation was already rejected.

So the current `ACTIVE` label is really anchored to:

- blocked-entry quality inversion inside a recurring shared pocket,

not to pocket existence by itself.

### 3. The blocked cohort is mixed, not a pure suppressor family

The blocked-reason split already narrows the boundary materially:

- largest blocked bucket: `stable_continuation_state`
- large suppressor buckets: `AGED_WEAK_CONTINUATION_GUARD`, `insufficient_evidence`
- sharper true-suppressor suspect: `insufficient_evidence`

So the active boundary is already known to sit on a mixed action-layer surface, not on one clean veto bucket.

### 4. Exact-surface D1 work strengthens one narrow carrier family, but only locally

Earlier exact-surface D1 work found a recurring non-age `insufficient_evidence` family on fixed exact subjects using:

- `action_edge`
- `confidence_gate`
- `clarity_raw`

That adds real support for a narrower carrier interpretation.
But those exact-surface results remain:

- bounded
- context-leaky
- non-authoritative

### 5. The first fresh late-2024 external transport test already failed unchanged transport

The late-2024 external-surface falsifier matters because it breaks any lazy escalation path.

The frozen D1 bank remained internally coherent, but it did **not** transport unchanged onto the first fresh late-2024 external surface.

So the evidence bank currently supports:

- internal exact-surface recurrence,

not:

- clean external portability.

### 6. A separate fixed local three-cohort read points to a broader harmful-pocket shell, but still only locally

The fixed three-cohort phase discriminator isolated the harmful `2024` pocket locally on:

- `bars_since_regime_change`
- and softer router-quality fields such as `action_edge`, `confidence_gate`, and `clarity_score`

This suggests that the harmful neighborhood may be structurally broader than one narrow `insufficient_evidence` carrier.
But that read is still fixed-subject local, not portable annual authority.

## Inferred

### 1. The right validation target is the label, not one favorite mechanism

The next slice should validate whether the **label** remains justified.
It should not start by assuming which inner carrier must win.

### 2. The likely honest outcomes are asymmetric

The current evidence shape makes three outcomes plausible:

1. keep `ACTIVE`, but narrow the wording to grouped annual proxy level only
2. keep the annual boundary `ACTIVE`, while parking carrier explanations as `WEAK`
3. downgrade the whole label to `WEAK` if the challenge surfaces undercut the grouped support too heavily

### 3. The next admissible move is a validation matrix, not a new wide helper

There is already enough retained material to do one bounded synthesis pass before opening any new helper-backed surface.

## Unverified

- whether the current `ACTIVE` label remains justified after integrating grouped support and external challenge surfaces in one place
- whether the correct end state is `ACTIVE narrowed`, `ACTIVE with weak carriers`, or `WEAK downgraded`
- whether any genuinely new helper is still needed after the synthesis pass

## Intended deliverables for this validation slice

1. one docs note with explicit support / narrowing / challenge matrix
2. one machine-readable verdict bundle for the `ACTIVE` boundary
3. optional read-only helper follow-up only if the synthesis pass still leaves one truly unresolved decisive question

## What changed and what did not

What changed:

- the next Edge Topology slice is now concretely opened as `ACTIVE boundary validation`
- the scope is locked against both broad annual reruns and premature runtime drift
- the kickoff now preserves the strongest support surfaces and the strongest challenge surfaces in one place

What did **not** change:

- no runtime behavior changed
- no existing Edge Topology artifact was rewritten here
- no `ACTIVE` label has yet been upgraded, downgraded, or reworded by this kickoff alone

# Archive-audit refactor index

This README is a navigation aid for the files currently present in this folder.
It does not replace or reinterpret the underlying audit or evidence documents.
For authoritative details, read the individual command packet files directly.

## What lives here

This folder currently preserves archive-audit command packets for candidate and shard review work.
The visible file pattern is primarily:

- `command_packet_archive_audit_*` — archive-related audit scope, constraints, and intended action

## Suggested reading order

1. Open the shard-level packet first if you need broad orientation.
2. Then open the candidate or batch packet for the exact archive subset you care about.
3. Use the filename date and candidate/batch label to follow the review sequence.

## Interpretation note

Treat these files as historical traceability records for archive-audit decisions.
Do not infer current retention or deletion status beyond what each individual packet explicitly records.

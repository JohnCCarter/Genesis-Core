meta:
  symbol: tBTCUSD
  timeframe: 1h
  snapshot_id: tBTCUSD_1h_2024-10-22_2025-10-01_v1
  warmup_bars: 150
  runs:
    strategy: grid
    resume: false
    max_trials: 1
    max_concurrent: 1
    use_sample_range: true
    sample_start: "2024-10-22"
    sample_end: "2024-11-01"

# Keep constraints permissive; the goal is config equivalence, not performance.
constraints:
  min_trades: 1
  min_profit_factor: 0.0
  max_max_dd: 1.0
  include_scoring_failures: true

parameters:
  thresholds:
    entry_conf_overall:
      type: fixed
      value: 0.30

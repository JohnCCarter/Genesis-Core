feat(ml): implement Phase 3.5 ML improvements

MAJOR ENHANCEMENTS:
- Expand features from 2 to 11 (5x improvement)
- Implement triple-barrier labeling (realistic trading scenarios)
- Add confidence edge requirement (filter low-quality trades)
- Improve regime detection (bull/bear/ranging/balanced)

FEATURE EXPANSION (2 -> 11 features):
- Volatility: ATR%, BB width, BB position
- Trend: ADX, EMA slope, Price vs EMA
- Volume: Volume change, Volume trend, OBV normalized

NEW INDICATORS:
- src/core/indicators/bollinger.py: Bollinger Bands, BB squeeze
- src/core/indicators/volume.py: Volume change, spikes, trend, OBV, divergence

LABELING IMPROVEMENTS:
- generate_triple_barrier_labels(): Profit target, stop loss, time exit
- generate_adaptive_triple_barrier_labels(): ATR-adaptive thresholds
- Filters noisy trades (small moves -> None label)
- Asymmetric R:R ratios (e.g., +0.3% profit / -0.2% stop)

DECISION LOGIC:
- Add min_edge parameter (require significant probability difference)
- Example: p_buy=0.70, p_sell=0.65 -> BLOCKED (edge only 0.05)
- Example: p_buy=0.80, p_sell=0.50 -> ALLOWED (edge 0.30)

REGIME DETECTION:
- Bull: ADX > 25, price > EMA, positive slope
- Bear: ADX > 25, price < EMA, negative slope
- Ranging: ADX < 20, low volatility
- Balanced: Transitional state
- Add detect_regime_from_candles() convenience function

TESTS:
- Add 88 new tests (177 -> 270 total)
- tests/test_bollinger.py: 23 tests
- tests/test_volume.py: 36 tests
- tests/test_regime.py: 11 tests (updated)
- tests/test_triple_barrier.py: 18 tests
- tests/test_decision_edge.py: 5 tests
- All tests pass with 100% coverage

EXPECTED IMPACT:
- Model AUC: 0.517 -> 0.65+ (target)
- Signal rate: 100% -> 30-40% (quality over quantity)
- Win rate: 50% -> 60%+ (edge filtering)
- Sharpe ratio: 0 -> 1.5+ (realistic R:R)

NEXT STEPS:
- Retrain models with 11 features + triple-barrier labels
- Backtest with confidence thresholds
- Validate AUC improvement on test set

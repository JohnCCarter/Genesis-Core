# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.env
.env.backup*
.env.*
**/.env
**/.env.*
**/.env.backup*
.venv/
.venv_*/
.venv-*/
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/*
!.vscode/mcp.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Testing
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.tox/
.cache
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# Logs
*.log
logs/
*.out

# Database
*.db
*.sqlite
*.sqlite3
# Optuna databases
test_optuna*.db
optuna_*.db

# Security & Sensitive files
.env
.env.local
.env.development
.env.test
.env.production
**/.nonce_tracker.json
dev.overrides.local.json
*.key
*.pem
*.p12
*.pfx
.secrets.baseline
bandit-report.json
bandit-report.txt

# Model provenance & validation (generated, don't commit)
**/*_provenance.json
**/*_holdout_indices.json
**/*_walk_forward.json
**/*_regime_validation.json
**/*_holdout_results.json

# Championship tickets (track in results/, gitignored)
championship_tickets/

# Champion backups (local safety copies, do not commit)
config/strategy/champions/backup/

# Runtime config (SSOT) - environment specific
config/runtime.json

# Dependencies
node_modules/

# Build artifacts
dist/
build/
*.egg-info/

# Temporary files
*.tmp
*.temp
.agent-locks/
tmp/*.txt
tmp/*.md

# Local scratch / canary tooling (never commit)
tmp/
tmp/**

# Local binaries / tunnel helpers (do not commit)
cloudflared*.exe
cloudflared*.exe.exe

# Temporary config artifacts (generated)
config/tmp/champion_current_as_cfg*.json

# Generated composite configs (local experiments)
config/tmp/composite_*.json

# Local analysis artifacts (do not commit)
config/tmp/trial_*_analysis.json

# Phase 3-5: Data files (historical candles, features, metadata)
# Two-layer architecture: Raw Lake + Curated
data/raw/**/*.parquet
data/curated/**/*.parquet
data/curated/**/*.feather
data/curated/**/*.json
data/candles/*.parquet
data/features/*.parquet
data/features/*.feather
data/archive/**
data/metadata/**/*.json
data/meta_labels/*.parquet

# Keep documentation in data directories
!data/DATA_FORMAT.md
!data/**/README.md

# Phase 3-5: ML Results (models, evaluations, calibrations, champion reports)
# Results (allow docs/archives)
results/
results/**/
!results/*.md
!results/**/README.md
!results/archive/**
!results/evaluation/
!results/evaluation/**/*.json

# Canary logs (append-only)
results/evaluation/**/*.jsonl
results/evaluation/**/*.ndjson

# CI artifacts
bandit-report.txt
bandit-report.json

# Keep results documentation and archives
!results/README.md
!results/**/README.md
!results/*.md
!results/archive/**

# Label cache (temporary, regenerate on demand)
cache/
cache/**

# Visualization outputs (regenerate on demand)
**/*_radar_comparison.png
**/*_champion_summary.png
**/*_heatmap.png
**/*_comparison_bars.png

# Fibonacci Exit Implementation Results
results/fibonacci_exits_*.json
results/fibonacci_exits_*.csv
results/fibonacci_exits_*.png

# Keep example files
!.env.example

# Archived configurations
config/optimizer/archive/

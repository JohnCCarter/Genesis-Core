# Regime Intelligence challenger family — experiment-map reselection packet-boundary

Date: 2026-03-30
Branch: `feature/ri-role-map-implementation-2026-03-24`
Status: `packet-boundary-defined / planning-only / no authorization`

## COMMAND PACKET

- **Mode:** `RESEARCH` — source: `docs/governance_mode.md`
- **Category:** `docs`
- **Risk:** `MED` — why: this packet determines the required packet type before any future runnable consideration could exist for the already selected regime-definition seam, but must remain narrower than pre-code, narrower than admissibility, narrower than YAML authoring, and narrower than launch or implementation authority.
- **Required Path:** `Quick`
- **Objective:** Define what packet boundary is required before any future runnable slice could exist for the already selected regime-definition seam.
- **Candidate:** `future packet boundary for bounded regime-definition slice`
- **Base SHA:** `c27add49`

### Scope

- **Scope IN:** one docs-only packet-boundary decision; explicit relation to the already existing generic SIGNAL lane admissibility packet; explicit statement of what remains fixed; explicit statement of what remains out of scope; explicit determination of the next admissible packet type only; explicit fail-closed non-authorization boundary.
- **Scope OUT:** no source-code changes; no config YAML creation; no validator/preflight/smoke/optimizer execution; no new slice; no launch subject; no admissibility refresh; no launch authorization; no implementation path; no changes under `src/**`, `config/**`, `scripts/**`, or `tests/**`; no fib reopening; no reopening of the closed slice8 upstream diagnostic path; no comparison/readiness/promotion/writeback.
- **Expected changed files:** `docs/governance/regime_intelligence_experiment_map_reselection_regime_definition_packet_boundary_2026-03-30.md`
- **Max files touched:** `1`

### Stop Conditions

- any wording that makes this packet a launch-readiness or execution-readiness claim
- any wording that treats the already existing generic SIGNAL admissibility packet as refreshed, extended, or re-authorized here
- any wording that turns the representative `3h` artifact into a launch subject
- any wording that creates YAML shape, search ranges, trial budget, selectors, validator/preflight plan, or execution guidance
- any wording that reopens fib or the closed slice8 upstream diagnostic path

## Purpose

This packet answers one narrow question only:

- what packet type must come next, if work continues, before any future runnable slice could be considered for the already selected regime-definition seam?

This packet is **planning-only governance**.

It does **not**:

- authorize launch
- authorize execution
- authorize validation activity
- authorize YAML creation
- authorize implementation
- establish runnable-slice readiness

## Governing basis

This packet is downstream of the following tracked artifacts:

- `docs/governance/regime_intelligence_upstream_diagnostic_path_closeout_2026-03-30.md`
- `docs/governance/regime_intelligence_experiment_map_reselection_roadmap_2026-03-30.md`
- `docs/governance/regime_intelligence_experiment_map_reselection_signal_segmentation_direction_packet_2026-03-30.md`
- `docs/governance/regime_intelligence_experiment_map_reselection_regime_definition_candidate_surface_packet_2026-03-30.md`
- `docs/governance/regime_intelligence_optuna_signal_lane_launch_admissibility_packet_2026-03-27.md`

Carried-forward meaning:

1. the current fixed-surface RI upstream diagnostic path on the slice8 surface remains closed in current tracked state
2. `SIGNAL / segmentation` has already been selected as the governing class orientation
3. `multi_timeframe.regime_intelligence.regime_definition.*` has already been selected as the single candidate surface
4. this packet must therefore decide only the next packet boundary, not any runnable step

## Non-reopen boundary

The closed slice8 upstream diagnostic path remains closed.

This packet does **not**:

- reopen that path
- extend that path
- derive a new diagnostic slice from that path
- convert that path into launch or implementation authority

The present decision is therefore a **packet-type boundary decision only**.

## 3h anchor preserved

This boundary remains anchored to the already selected `3h` regime-definition seam.

Representative artifact reference only:

- `config/optimizer/3h/ri_challenger_family_v1/tBTCUSD_3h_ri_signal_regime_definition_slice1_2024_v1.yaml`

Meaning:

- this artifact is cited only as the representative anchor for the chosen already implemented surface
- it is **not** made launchable, admissible-for-execution, or runnable by this packet
- timeframe, authority surface, and runtime contract remain unchanged

## Relationship to existing SIGNAL admissibility

The existing packet:

- `docs/governance/regime_intelligence_optuna_signal_lane_launch_admissibility_packet_2026-03-27.md`

already defines **generic lane-level research admissibility** for future SIGNAL work.

This packet does **not**:

- replace that packet
- refresh that packet
- extend that packet
- convert that packet into launch authorization for the current seam

Instead, this packet records the narrower conclusion that the current work no longer needs another **generic** admissibility-definition step before continuing in docs-only governance.

## Boundary decision

### What is not required next

For this already implemented and already config-represented seam, the next packet does **not** need to be:

- an implementation-enablement packet
- a generic SIGNAL admissibility-refresh packet
- a launch-authorization packet
- a YAML-authoring packet

Why:

- the chosen seam already exists in the runtime/config-authority surface
- the chosen seam already has a representative `3h` research artifact in the repository
- generic SIGNAL research-level admissibility has already been defined at lane level

This is a narrow seam-specific conclusion only.

It does **not** imply that all future regime-definition research is implementation-free or launch-ready.

### What the next admissible step may be

If work continues, the next admissible step may only be:

- a **separate pre-code packet** for one exact bounded regime-definition slice

That future packet, if opened later, must still be docs-only and bounded.

It must define only the exact pre-code boundary for one future slice and must not itself authorize execution.

## What remains fixed

The following remain fixed before any future pre-code packet could exist:

- fib lane closed
- current slice8 upstream diagnostic path closed
- chosen class fixed: `SIGNAL / segmentation`
- chosen candidate surface fixed: `multi_timeframe.regime_intelligence.regime_definition.*`
- objective/scoring class fixed
- EV surface fixed
- confidence surface fixed
- exit/management surface fixed
- family-registry and family-admission rules fixed
- comparison closed
- readiness closed
- promotion closed
- champion/default writeback closed

## What remains out of scope

Still out of scope beyond this packet:

- any runnable slice
- any launch subject
- any YAML creation or YAML editing
- any search range, grid, step size, or trial budget
- any validator/preflight/smoke path
- any execution path
- any implementation-enablement work
- any launch authorization
- any comparison/readiness/promotion/writeback framing

## Explicit non-authorization boundary

This packet does **not** constitute:

- launch authorization
- execution approval
- validation approval
- runnable-slice readiness
- admissibility refresh
- implementation approval

Its sole purpose is to determine the required packet type for any future runnable consideration on the already selected regime-definition seam.

## Bottom line

The packet boundary is now defined as follows:

- no implementation-enablement packet is required for the already implemented, already config-represented `multi_timeframe.regime_intelligence.regime_definition.*` seam
- no new generic SIGNAL admissibility packet is required
- the next admissible step, if work continues, may only be a **separate pre-code packet for one exact bounded regime-definition slice**

Nothing in this packet authorizes launch, execution, validation activity, YAML creation, or direct movement into a runnable lane.

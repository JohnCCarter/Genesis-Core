# RI policy router insufficient-evidence late 2024 recurrence falsifier precode packet

Date: 2026-05-04
Branch: `feature/next-slice-2026-04-29`
Mode: `RESEARCH`
Status: `föreslagen / pre-code packet / research-evidence only / read-only / non-authoritative / no behavior change`

This packet opens one bounded follow-up to the completed July `2024` within-envelope falsifier.
It chooses the smallest repo-visible non-March continuation of that line:

- one exact **late-2024 recurrence falsifier** on the already row-locked `insufficient_evidence` subset inside the fixed `2024-11-27 .. 2024-12-03` regression pocket

This packet does **not** reopen March `2021` / March `2025` as the primary subject.
It does **not** reopen July `2024` as the primary subject.
It does **not** reopen the closed `2024` versus `2020` harmful-vs-correct counterfactual screen as an active control branch.
It does **not** authorize runtime/default/policy/config/promotion work.

## COMMAND PACKET

- **Category:** `obs`
- **Mode:** `RESEARCH` — source: live branch `feature/next-slice-2026-04-29`
- **Risk:** `LOW` — why: this packet freezes one future read-only evidence slice on already row-locked late-2024 rows and already-established July reference thresholds only.
- **Required Path:** `Lite`
- **Lane:** `Research-evidence` — why this is the cheapest admissible lane now: the July `2024` / frozen `2020` surface is already closed as bounded evidence, the late-2024 pocket is already row-locked, and the next honest move is to pressure-test portability on one fixed non-March subject rather than reopen annual mining or runtime speculation.
- **Objective:** define one future read-only slice that tests whether the bounded July `2024` age-plus-weak-signal read has any portable recurrence on one exact late-2024 non-March subject without reopening the closed `2024` versus `2020` counterfactual branch.
- **Candidate:** `exact late-2024 insufficient_evidence recurrence falsifier`
- **Base SHA:** `2c9522eaecee5392e294eb0c0a1a6aac9d0d51a5`
- **Skill Usage:** `python_engineering` — invoked for the bounded helper implementation on this slice; no broader skill coverage is claimed

## Why this packet exists

The completed July `2024` within-envelope falsifier established one bounded conjunction on the exact July `2024` / frozen `2020` surface:

- `bars_since_regime_change <= 166`
- plus one weak-signal ceiling from the same admitted field family:
  - `action_edge <= 0.034334`, or
  - `confidence_gate <= 0.517167`, or
  - `clarity_score <= 37`

But the next admissible step in the working contract is **not** another July reread.
It is one additional fixed non-March subject falsifier asking whether that bounded read survives outside the exact July `2024` / frozen `2020` pair.

The already-locked late-2024 `insufficient_evidence` subset is the cheapest honest subject for that pressure test because:

1. it is already row-locked,
2. it is already split cleanly away from the mixed `AGED_WEAK_CONTINUATION_GUARD` sibling rows,
3. it already sits in the same weak edge/confidence/clarity family as the July target,
4. its local `fwd_16` truth is weak rather than favorable, making it a true portability pressure test rather than a second positive confirmation surface.

This packet freezes that question before any new helper runs.
It exists specifically to prevent `recurs` from drifting between incompatible meanings.

## Exact research question

The future execution slice must keep the following two questions separate and report them separately:

### 1. Exact-transport falsifier

If the three bounded July `2024` two-field survivors are transported **unchanged** onto the exact late-2024 subject, does any one of them still select most late-2024 target rows while rejecting the fixed late-2024 anti-target rows?

The transported July rule variants are fixed to:

1. `bars_since_regime_change <= 166` AND `action_edge <= 0.034334`
2. `bars_since_regime_change <= 166` AND `confidence_gate <= 0.517167`
3. `bars_since_regime_change <= 166` AND `clarity_score <= 37`

If none of those three fixed transported rules survive, the exact-transport portability claim is falsified.

### 2. Weak-signal recurrence read

Separately, do the July weak-signal ceilings re-materialize on the late-2024 target rows even though the July absolute age ceiling does not?

This is descriptive only.
It may report row counts for:

- `action_edge <= 0.034334`
- `confidence_gate <= 0.517167`
- `clarity_score <= 37`
- and the three-way intersection of those weak-signal ceilings

But this weak-signal recurrence report may **not** rescue a failed exact-transport result, and it may **not** be promoted into a new threshold-search or runtime-authoritative rule.

If answering either question would require fresh thresholds, annual widening, March-reopen, July-reopen as primary subject, or a revived `2024` versus `2020` harmful-vs-correct control role, stop and open a new governed packet.

## Fixed subject lock

### Exact late-2024 target rows (`5`)

These are the already-locked `insufficient_evidence` rows from the late-2024 regression pocket:

- `2024-11-29T09:00:00+00:00`
- `2024-11-29T18:00:00+00:00`
- `2024-11-30T03:00:00+00:00`
- `2024-12-01T15:00:00+00:00`
- `2024-12-02T00:00:00+00:00`

Shared target context:

- year = `2024`
- zone = `low`
- candidate = `LONG`
- absent action = `LONG`
- enabled action = `NONE`
- switch reason = `insufficient_evidence`
- selected policy = `RI_no_trade_policy`

Observed local truth already established on the fixed pocket surface:

- `fwd_16` mean: `-0.855705%`
- `fwd_16 > 0` share: `0%`
- `bars_since_regime_change` mean: `281.4`
- `action_edge` mean: `0.027769`
- `confidence_gate` mean: `0.513884`
- `clarity_score` mean: `36.2`

### Fixed late-2024 anti-target rows (`6`)

These rows remain fixed local anti-target context for the future falsifier.

Pocket sibling rows with a different blocker reason (`4`):

- `2024-11-28T15:00:00+00:00`
- `2024-11-29T00:00:00+00:00`
- `2024-11-30T12:00:00+00:00`
- `2024-11-30T21:00:00+00:00`

Shared sibling context:

- absent action = `LONG`
- enabled action = `NONE`
- switch reason = `AGED_WEAK_CONTINUATION_GUARD`

Nearby `stable_continuation_state` context rows (`2`):

- true displacement row: `2024-12-01T00:00:00+00:00`
- stable blocked context row: `2024-12-01T06:00:00+00:00`

These two nearby stable rows are already documented and remain part of the fixed local anti-target surface.

### Fixed local envelope

- `2024-11-27T15:00:00+00:00` -> `2024-12-03T00:00:00+00:00`
- exact fixed local membership = `5` target + `6` anti-target rows
- additional unlabeled local rows inside the envelope = `0`

## July reference lock

The July reference slice remains frozen exactly as completed.
It is not reopened as the primary subject in the future execution slice.
It is used only as the source of the three already-locked transported rule variants listed above.

The future helper must treat these July-derived thresholds as fixed constants rather than as candidate values to be re-searched.

## Allowed candidate surface

The future helper is closed to the following operations only:

1. apply the three fixed transported July rule variants to the exact late-2024 target and anti-target rows
2. report weak-signal recurrence counts for the three fixed weak-signal ceilings and their three-way intersection
3. report descriptive cohort summaries for the fixed target and anti-target groups on already-emitted decision-time and offline observational fields

Not allowed:

- fresh threshold search
- new fields
- equality matching
- annual widening
- a new `2020` control role
- fresh raw-source mining beyond the already-locked late-2024 artifact surface
- runtime-authoritative interpretation

## Survival rule for a later execution slice

### Exact-transport portability

One transported July rule variant counts as a bounded late-2024 portability survivor **only** if all of the following remain true:

1. it selects at least `4 / 5` exact late-2024 target rows
2. it selects at most `1 / 6` fixed late-2024 anti-target rows
3. the helper proves exact row-lock integrity on the full `5 + 6` surface

If none of the three transported July rule variants satisfy those conditions, the exact-transport verdict must fail closed to `transport_falsified`.

### Weak-signal recurrence

The weak-signal recurrence read is descriptive only.
It may report row counts and selection rates, but it cannot produce a `survivor` verdict and cannot elevate any late-2024 weak-signal match into new rule authority.

## Required row-lock and assertion discipline

Before any later helper may evaluate the late-2024 surface, it must assert all of the following:

- the exact target set is the five timestamps listed above
- the exact anti-target set is the six timestamps listed above
- the full fixed local envelope contains exactly those `11` rows and no unlabeled extras
- blocker reasons and action-pair signatures remain exactly as already documented
- the July reference rules remain fixed to the three explicit transported variants above

If any row-lock, count, or signature assertion fails, the later helper must fail closed rather than widen, substitute, or infer.

## Planned future output paths

If a later execution slice is separately approved, its exact output paths should be:

- analysis helper: `scripts/analyze/ri_policy_router_insufficient_evidence_late_2024_recurrence_falsifier_20260504.py`
- JSON artifact: `results/evaluation/ri_policy_router_insufficient_evidence_late_2024_recurrence_falsifier_2026-05-04.json`
- analysis note: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_late_2024_recurrence_falsifier_2026-05-04.md`

## Scope

- **Scope IN:**
  - `docs/decisions/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_late_2024_recurrence_falsifier_precode_packet_2026-05-04.md`
  - `scripts/analyze/ri_policy_router_insufficient_evidence_late_2024_recurrence_falsifier_20260504.py`
  - `results/evaluation/ri_policy_router_insufficient_evidence_late_2024_recurrence_falsifier_2026-05-04.json`
  - `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_late_2024_recurrence_falsifier_2026-05-04.md`
  - `GENESIS_WORKING_CONTRACT.md` only if the future slice becomes the pointer-only next admissible-step anchor
- **Scope OUT:**
  - `src/**`
  - `tests/**`
  - `config/**`
  - `results/backtests/**`
  - March `2021` / March `2025` as primary subject
  - July `2024` as a reopened primary subject rather than a locked threshold reference
  - any reopening of the closed `2024` versus `2020` harmful-vs-correct counterfactual branch as active control logic
  - fresh annual discovery, new subject mining, or new threshold search
  - runtime/default/policy/family/champion/promotion/readiness surfaces
- **Expected changed files:** `4-5`
- **Max files touched:** `5`

## Validation requirements for a later execution slice

- `get_errors` on the packet/helper/note and any touched working-contract file
- exact late-2024 row-lock proof for the `5 + 6` surface
- successful helper execution against the fixed late-2024 surface only
- emitted artifact schema check proving exact cohort membership and deterministic ordering
- `ruff check` on the new helper and any touched Python file
- `pre-commit run --files` on the touched files
- manual diff review confirming that the exact-transport verdict and weak-signal recurrence read remain separate and non-authoritative

## Stop conditions

- the future slice needs fresh thresholds instead of the locked July reference thresholds
- the future slice needs a revived `2020` active control role to make its claim work
- the future slice starts widening beyond the exact late-2024 `5 + 6` surface
- the future slice starts implying runtime tuning, policy changes, default changes, promotion, or readiness
- the future slice lets a weak-signal recurrence read rescue a failed exact-transport falsifier

## Output required from a later execution slice

- one deterministic JSON summary artifact on the exact late-2024 surface
- one human-readable analysis note stating clearly:
  - whether exact July transport failed or survived, and
  - whether the weak-signal half re-materialized descriptively on the late-2024 target rows
- exact validation outcomes

## What this packet does not authorize

This packet does **not** authorize:

- execution by itself
- new threshold discovery
- reopening the closed `2024` versus `2020` counterfactual screen as active logic
- runtime threshold changes
- router-policy changes
- default-behavior changes
- concept promotion of the July conjunction

Bottom line for this packet only:

> the next smallest honest pressure test is one exact late-2024 portability falsifier that freezes both the transported July rules and the separate weak-signal recurrence read before any new helper is allowed to run.

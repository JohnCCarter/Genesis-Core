# RI policy router insufficient-evidence 2022-06 weak-signal holdout precode packet

Date: 2026-05-04
Branch: `feature/next-slice-2026-04-29`
Mode: `RESEARCH`
Status: `föreslagen / pre-code packet / research-evidence only / read-only / non-authoritative / no behavior change`

This packet opens one bounded follow-up to the completed July `2024` within-envelope falsifier and the completed late-2024 recurrence falsifier.
It chooses the smallest repo-visible second non-March subject beyond those already closed surfaces:

- one exact **2022-06 weak-signal holdout** on a fixed `5 + 4` low-zone `insufficient_evidence` / stable-context surface

This packet does **not** reopen March `2021` / March `2025` as the primary subject.
It does **not** reopen July `2024` as the primary subject.
It does **not** reuse the completed late-2024 `5 + 6` subject.
It does **not** reopen the closed `2024` versus `2020` harmful-vs-correct counterfactual screen as an active control branch.
It does **not** authorize runtime/default/policy/config/promotion work.

## COMMAND PACKET

- **Category:** `obs`
- **Mode:** `RESEARCH` — source: live branch `feature/next-slice-2026-04-29`
- **Risk:** `LOW` — why: this packet freezes one future read-only evidence slice on one fresh fixed non-March subject plus one docs-only synthesis/parking closeout derived only from committed anchor notes and the emitted bounded artifact.
- **Required Path:** `Lite`
- **Lane:** `Research-evidence` — why this is the cheapest admissible lane now: the July `2024` / frozen `2020` and late-2024 subjects are already closed as bounded evidence, and the remaining honest question is whether the weak-signal family now recurs descriptively on one second fresh fixed subject before the line is parked.
- **Objective:** define one future read-only slice that tests whether the fixed July weak-signal family recurs descriptively on one exact `2022-06` subject while the July age clause remains fully falsified, and then materialize one docs-only synthesis/parking closeout if that bounded read stays non-authoritative.
- **Candidate:** `exact 2022-06 weak-signal holdout plus docs-only parking closeout`
- **Base SHA:** `b1fc5cfb5d938b5bd3b95d133d0f769248241a48`
- **Skill Usage:** `python_engineering` — invoked for the bounded helper implementation on this slice; no broader skill coverage is claimed

## Why this packet exists

The completed July `2024` within-envelope falsifier established one bounded conjunction on the exact July `2024` / frozen `2020` surface:

- `bars_since_regime_change <= 166`
- plus one weak-signal ceiling from the same admitted field family:
  - `action_edge <= 0.034334`, or
  - `confidence_gate <= 0.517167`, or
  - `clarity_score <= 37`

The completed late-2024 recurrence falsifier then tightened that read:

1. unchanged transport of the full July conjunction failed completely,
2. the weak-signal ceilings recurred descriptively only, and
3. the July conjunction remained exact-surface evidence rather than portable authority.

So the next honest question is no longer another reread of July `2024` or late-2024.
It is one second fixed non-March holdout that asks whether the weak-signal family now recurs cleanly on a fresh exact subject while the July age clause remains fully falsified there too.

`2022-06` is the cheapest honest subject for that pressure test because:

1. it is fresh and outside all current hard exclusions,
2. it is already packet-shaped on the current repo-visible action-diff surface,
3. it carries one exact `5`-row `insufficient_evidence` target cluster plus one exact `4`-row stable anti-target set,
4. it contains no unlabeled extras inside the chosen local envelope,
5. its target cluster is locally weak while the stable anti-target rows are locally positive, so the read is informative without reopening the parked `2024` versus `2020` control logic.

This packet freezes that question before any new helper runs.
It also pre-authorizes one docs-only synthesis/parking closeout derived only from the completed bounded evidence chain if the future result still remains observational and non-authoritative.
That closeout may park only this bounded translation-lane chain.
It may not claim global exhaustiveness, reopen a primary subject implicitly, or restore authority to the falsified July transport rule.

## Exact research question

The future execution slice must keep the following two questions separate and report them separately:

### 1. Exact-transport falsifier

If the three bounded July `2024` two-field survivors are transported **unchanged** onto the exact `2022-06` subject, does any one of them still select most target rows while rejecting the fixed stable anti-target rows?

The transported July rule variants are fixed to:

1. `bars_since_regime_change <= 166` AND `action_edge <= 0.034334`
2. `bars_since_regime_change <= 166` AND `confidence_gate <= 0.517167`
3. `bars_since_regime_change <= 166` AND `clarity_score <= 37`

If none of those three fixed transported rules survive, the exact-transport portability claim is falsified again on this second holdout subject.

### 2. Weak-signal holdout read

Separately, do the July weak-signal ceilings re-materialize on the exact `2022-06` target rows even though the July absolute age ceiling does not?

This is descriptive only.
It may report row counts for:

- `action_edge <= 0.034334`
- `confidence_gate <= 0.517167`
- `clarity_score <= 37`
- and the three-way intersection of those weak-signal ceilings

But this weak-signal holdout read may **not** rescue a failed exact-transport result, and it may **not** be promoted into a new threshold-search or runtime-authoritative rule.
It also may **not** be written as if a second descriptive recurrence restores transport validity for the July conjunction.

If answering either question would require fresh thresholds, annual widening, March-reopen, July-reopen as primary subject, a revived `2024` versus `2020` control role, or any runtime/policy interpretation, stop and open a new governed packet.

## Fixed subject lock

### Exact `2022-06` target rows (`5`)

These are the exact low-zone `insufficient_evidence` rows to be frozen as the new target subject:

- `2022-06-24T03:00:00+00:00`
- `2022-06-24T21:00:00+00:00`
- `2022-06-25T06:00:00+00:00`
- `2022-06-25T15:00:00+00:00`
- `2022-06-26T00:00:00+00:00`

Shared target context:

- year = `2022`
- zone = `low`
- candidate = `LONG`
- absent action = `LONG`
- enabled action = `NONE`
- switch reason = `insufficient_evidence`

Observed local truth from the repo-visible surface to be revalidated by the helper:

- `fwd_16` mean currently reads as weak / negative on this exact target cluster
- the target rows sit in the same weak edge / weak confidence / lower clarity family already surfaced on July `2024` and late-2024

### Exact `2022-06` anti-target rows (`4`)

These rows are frozen as the stable anti-target set for the holdout.
They are already visible on the same action-diff surface and contain no unlabeled extras inside the chosen local envelope.

True displacement rows (`2`):

- `2022-06-23T03:00:00+00:00`
- `2022-06-23T12:00:00+00:00`

Stable blocked context rows (`2`):

- `2022-06-23T09:00:00+00:00`
- `2022-06-23T18:00:00+00:00`

Shared anti-target context:

- switch reason = `stable_continuation_state`
- displacement rows use `NONE -> LONG`
- blocked context rows use `LONG -> NONE`

### Fixed local envelope

- `2022-06-23T03:00:00+00:00` -> `2022-06-26T00:00:00+00:00`
- exact fixed local membership = `5` target + `4` anti-target rows
- additional unlabeled local rows inside the envelope = `0`

## July reference lock

The July reference slices remain frozen exactly as completed.
They are not reopened as the primary subject in the future execution slice.
They are used only as the source of the three already-locked transported rule variants and the corresponding weak-signal ceilings.

The future helper must treat these July-derived thresholds as fixed constants rather than as candidate values to be re-searched.

## Allowed candidate surface

The future helper is closed to the following operations only:

1. apply the three fixed transported July rule variants to the exact `2022-06` target and anti-target rows
2. report weak-signal holdout counts for the three fixed weak-signal ceilings and their three-way intersection
3. report descriptive cohort summaries for the fixed target and anti-target groups on already-admitted decision-time fields plus offline observational candle metrics
4. if the evidence remains bounded and non-authoritative, support one docs-only synthesis/parking note derived from the completed July, late-2024, and `2022-06` chain

Not allowed:

- fresh threshold search
- new fields
- equality matching
- annual widening
- a new `2020` control role
- raw-source mining beyond the exact `2022` action-diff surface plus curated candles already used elsewhere in this line
- runtime-authoritative interpretation

## Survival rule for a later execution slice

### Exact-transport portability

One transported July rule variant counts as a bounded `2022-06` transport survivor **only** if all of the following remain true:

1. it selects at least `4 / 5` exact `2022-06` target rows
2. it selects at most `1 / 4` fixed `2022-06` anti-target rows
3. the helper proves exact row-lock integrity on the full `5 + 4` surface

If none of the three transported July rule variants satisfy those conditions, the exact-transport verdict must fail closed to `transport_falsified`.

### Weak-signal holdout read

The weak-signal holdout read is descriptive only.
It may report row counts and selection rates, but it cannot produce a `survivor` verdict and cannot elevate any `2022-06` weak-signal match into new rule authority.

## Required row-lock and assertion discipline

Before any later helper may evaluate the `2022-06` surface, it must assert all of the following:

- the exact target set is the five timestamps listed above
- the exact anti-target set is the four timestamps listed above
- the full fixed local envelope contains exactly those `9` rows and no unlabeled extras
- blocker reasons and action-pair signatures remain exactly as already documented
- the July reference rules remain fixed to the three explicit transported variants above

If any row-lock, count, or signature assertion fails, the later helper must fail closed rather than widen, substitute, or infer.

## Planned future output paths

If a later execution slice is separately approved, its exact output paths should be:

- analysis helper: `scripts/analyze/ri_policy_router_insufficient_evidence_2022_06_weak_signal_holdout_20260504.py`
- JSON artifact: `results/evaluation/ri_policy_router_insufficient_evidence_2022_06_weak_signal_holdout_2026-05-04.json`
- analysis note: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_2022_06_weak_signal_holdout_2026-05-04.md`
- docs-only synthesis note: `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_translation_parking_synthesis_2026-05-04.md`

## Scope

- **Scope IN:**
  - `docs/decisions/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_2022_06_weak_signal_holdout_precode_packet_2026-05-04.md`
  - `scripts/analyze/ri_policy_router_insufficient_evidence_2022_06_weak_signal_holdout_20260504.py`
  - `results/evaluation/ri_policy_router_insufficient_evidence_2022_06_weak_signal_holdout_2026-05-04.json`
  - `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_2022_06_weak_signal_holdout_2026-05-04.md`
  - `docs/analysis/regime_intelligence/policy_router/ri_policy_router_insufficient_evidence_translation_parking_synthesis_2026-05-04.md`
  - `GENESIS_WORKING_CONTRACT.md` only if the completed slice becomes the parked truthful next-step anchor
- **Scope OUT:**
  - `src/**`
  - `tests/**`
  - `config/**`
  - `results/backtests/**`
  - March `2021` / March `2025` as primary subjects
  - July `2024` as a reopened primary subject rather than a locked threshold reference
  - reuse of the completed late-2024 `5 + 6` subject as the new holdout
  - any reopening of the closed `2024` versus `2020` harmful-vs-correct counterfactual branch as active control logic
  - fresh annual discovery, new subject mining, or new threshold search
  - runtime/default/policy/family/champion/promotion/readiness surfaces
- **Expected changed files:** `5-6`
- **Max files touched:** `6`

## Validation requirements for a later execution slice

- `get_errors` on the packet/helper/note/synthesis note and any touched working-contract file
- exact `2022-06` row-lock proof for the `5 + 4` surface
- successful helper execution against the fixed `2022-06` surface only
- emitted artifact schema check proving exact cohort membership and deterministic ordering
- `ruff check` on the new helper and any touched Python file
- `pre-commit run --files` on the touched files
- manual diff review confirming that exact-transport falsification, weak-signal holdout, and final synthesis/parking wording remain separate and non-authoritative

## Stop conditions

- the future slice needs fresh thresholds instead of the locked July reference thresholds
- the future slice needs to revive the parked `2024` versus `2020` control logic to make its claim work
- the future slice starts widening beyond the exact `2022-06` `5 + 4` surface
- the future slice starts implying runtime tuning, policy changes, default changes, promotion, or readiness
- the future slice lets a descriptive weak-signal holdout read rescue a failed exact-transport falsifier

## Output required from a later execution slice

- one deterministic JSON summary artifact on the exact `2022-06` surface
- one human-readable analysis note stating clearly:
  - whether exact July transport failed or survived, and
  - whether the weak-signal family re-materialized descriptively on the `2022-06` target rows while staying out of the fixed anti-target rows
- one docs-only synthesis/parking note that states the honest chain-wide bounded read without promoting any field or rule
- exact validation outcomes

## What this packet does not authorize

This packet does **not** authorize:

- execution by itself
- new threshold discovery
- reopening the closed `2024` versus `2020` counterfactual screen as active logic
- runtime threshold changes
- router-policy changes
- default-behavior changes
- concept promotion of the July conjunction or the weak-signal family

Bottom line for this packet only:

> the next smallest honest pressure test is one exact `2022-06` holdout that checks whether the July weak-signal family now recurs descriptively on a second fixed non-March subject while the July age clause remains fully falsified, followed by a docs-only parking closeout if the line still remains non-authoritative.
